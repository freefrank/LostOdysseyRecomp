# v0.5.0 渲染、性能与发布交接 — 2026-09-09

## 从这里继续

用户最新要求是先总结问题、写 handoff，然后在新 session 继续。本轮停在资料交接，没有开始新诊断、代码修改或构建。仓库为 `C:\Users\freefrank\ownCloud\Git\LostOdysseyRecomp`。

用户已明确：**暂缓 Reddit 帖子，修复此前提到的问题后再发；查看日志定位错误阴影，尽可能排查同类错误，并将 shader 日志独立出来。** 另报告 4K 游戏只有 25–30 FPS。此前要求记录 DPI、偶发全屏不铺满和 Alt+Enter 切换窗口／全屏。

**最新优先级与目标：下表六项全部 Urgent，必须覆盖 v0.5.0 发布。** 这条用户指令覆盖此前将 DPI／窗口问题留到后续版本的安排；修复和必要验证完成前，GitHub Release 与 Reddit 均保持暂停。本轮仍按用户要求停在handoff，开发在新session接续。

GitHub v0.5.0 的 CI 和产物核验已经完成，Release 仍为草稿；这与 Reddit 暂缓是两个状态。当前不要公开草稿，也不要把未修复问题包装成已经完成的宣传成果。用户没有取消整个 v0.5.0 milestone。

## 当前问题清单

| 问题／需求 | 已知事实 | 未完成事项 |
| --- | --- | --- |
| 4K 下阴影／地面闪烁、大块黑地面 | D3D12、Map16 同进程同机位对照中，选择 TAA 时出现，AA Off 六秒片段未见对应黑地面 | 对齐日志、实际 draw／shader／资源状态；找根因并修复，审查同类路径 |
| 4K 性能仅 25–30 FPS | 用户实玩报告；后续运行日志也记录约 26–27 FPS | 实际 benchmark／CPU 与 GPU 分段耗时尚未进行，不能归因于 TAA 或某个 shader |
| 游戏输出受 DPI 影响 | 150% 显示器上内部／客户区 4K，游戏截图却是 2560×1440 | 确认渲染线程 DPI 上下文与最终 swapchain 尺寸，修复物理像素一致性 |
| 全屏偶尔未填满屏幕 | 用户观察，尚未单独复现定位 | 区分窗口边界错误、输出尺寸错误和合理宽高比黑边 |
| Alt+Enter 切换窗口／全屏 | 新需求，未实现 | 切换时恢复窗口位置和大小，处理按键重复、DPI／resize 生命周期 |
| shader 日志独立 | 用户明确要求，未实现 | 从通用日志分流 shader 诊断，保证错误可追溯及反馈包包含所需日志 |

DPI 最初被用户列为后续 TODO；其后用户明确将以上全部需求提升为 Urgent、目标 v0.5.0。这六项都是当前发布前待完成项，不移到 Future 或 v0.5.1；仍需分别记录实现、验证和用户验收，不能因进入milestone便勾选完成。

## 1. 阴影／黑地面证据

先读 [4K 对照报告](../../out/v0.5.0/reddit-launch/capture/REPORT-4K.md)。证据目录统一为 `out/v0.5.0/reddit-launch/capture/`。

- 可执行文件：`capture/package/LostOdysseyRecomp-windows-x64-v0.5.0/LostOdysseyRecomp.exe`，本地正式 v0.5.0 包，source `86ba2c1`。
- EXE SHA256：`acee4e578fac2dad9721e4a4d6e781fcd1615ef0b54451643251c86c655494a7`。**不是后来 CI 的 EXE**，对比时不要混淆。
- 原诊断 PID 53056，已结束；运行目录 `capture/run-4k-01/`，有 `runtime.log`、`settings.ini`、`session.json` 和原始截图。
- 后端 D3D12，GPU RTX 5080。原生 `user06/save.bin` 读取后到 Map16／Uhra Main Street。无传送或剧情写入。
- 同一静止相机／位置、同一进程：先 TAA 3，之后使用真实设置菜单保存 AA Off 0。日志 `[663.877] settings saved` 确认内部2160、输出3840×2160、AA0。
- 两段各六秒、3840×2160 的视频：`taa-4k-stationary.mp4` 和 `aa-off-4k-stationary.mp4`。
- TAA 段地面在正常纹理与大片黑色之间交替；Off 段未见对应症状。**不要写成 Off 也有黑帧。**
- 相同地面 ROI `[1500,1800,2100,2050]`：TAA 180 个编码帧中 100 帧均值低于30，Off 为0／180；完整统计在 `flicker-comparison.json`。
- 图像：`taa-flicker-02.png` 正常、`taa-flicker-06.png` 黑地面；`off-flicker-02.png`、`off-flicker-06.png` 正常，主代理已经查看。

录像每段实际收到167个 WGC回调，30 FPS 容器通过重复帧保留真实时长，没有插帧或变速。**编码帧数、回调数、ROI 统计均不是游戏 FPS benchmark。** 原诊断配置本身为 `frame_rate=30`。

### 因果边界

现有证据只证明该场景与所选 AA 模式有关，**未定位失败的 shader、draw 或资源；未证明 TAA history 算法是根因**。原 scene-AA／temporal 日志的有限帧区间在标题／加载期间已经耗尽，没有覆盖录像中的实际 history readiness/reuse。不能把 UI 的“TAA”选项当作该帧 history 复用成功。

此时没有新定位出的 shader hash／PSO／错误阴影 draw，也没有新的同类错误清单。不要根据大块黑地面外观直接判为 shader 翻译错误、DPI 错误或 GPU 驱动问题。

后续可沿实际错误帧追查：选中的 color/depth/history 资源、尺寸、格式、读写生命周期、history reset／resize、jitter／投影数据和对应 draw／shader。上述只是建议调查方向，不是已证实的异常。确定机制后，再用同一不变量检查其他路径／后端，区分静态风险与已复现缺陷。

## 2. 用户观察运行与性能线索

用户要求再次运行并亲自观察，曾交接 PID49760／HWND85790386 到物理主屏。主代理用 AppActivate 并核对前台窗口所属 PID 完成前台交接；自动输入、录制和自动退出已停止。

**交接文档编写时只读刷新发现：PID49760 已不存在，日志末行 `[232.685] video: window closed, exiting`。主代理没有结束该进程，不要继续把它当作正在运行，也不要自动重启。**

运行资料：

- [runtime.log](../../out/v0.5.0/reddit-launch/capture/run-user-observe-01/runtime.log)
- [console.log](../../out/v0.5.0/reddit-launch/capture/run-user-observe-01/console.log)
- [最终 settings.ini](../../out/v0.5.0/reddit-launch/capture/run-user-observe-01/settings.ini)
- [启动及前台交接 session.json](../../out/v0.5.0/reddit-launch/capture/run-user-observe-01/session.json)

启动时为 TAA3、内部2160、请求输出3840×2160。用户操作后，最终 `settings.ini` 是 `antialiasing=1`、`frame_rate=60`、`internal_resolution=2160`、3840×2160、`graphics_backend=0`、`window_mode=0`；日志 `[164.535] settings saved ... AA=1` 与最终文件一致。因此 **session.json 的初始 TAA 条件不能代表后续全部时间段**。

末段场景日志 `[173.516–232.171]` 的 heartbeat 多为25.9–29.7 FPS，约3960–4063 draws/frame；菜单阶段曾有60 FPS。它与用户“4K只有25–30 FPS”的观察相符，但仍是现有低频日志，未采集正式帧时间／CPU/GPU分段。不能直接宣布 CPU 瓶颈、GPU 瓶颈、draw call 瓶颈或 TAA 性能回归。

用户明确要求性能比较使用实际 benchmark，不能用低频日志估算替代正式结论。后续先确认 AA、帧率上限、场景、内部分辨率、实际呈现尺寸、是否前台、后台其他负载，保留旧基线，只测需要的新条件。该用户运行在物理主屏150% DPI上，实际 presented output 没有测量，不能称其已证实全链路原生4K。

环境仅隔离设置／存档／缓存：`LO_AUDIO_MUTE=1`，`LO_LOG_FILE` 指向上述日志，`LO_SHADER_CACHE_DIR` 指向 `capture/run-01/shader-cache`。初始 Start/A/A 之后输入全部释放。不要拿测试静音改变用户正常配置。

## 3. DPI 与全屏

同一个原诊断窗口：

| 条件 | Win32 原生客户区 | 游戏 presented 截图 |
| --- | --- | --- |
| DISPLAY1，物理4K屏，150%／DPI144 | 3840×2160 | 2560×1440，`run-4k-01/shot_1230.png` |
| DISPLAY2，100%／DPI96 | 3840×2160 | 3840×2160，`run-4k-01/shot_5650.png` |

**DISPLAY2 是现有 Parsec 虚拟1024×768显示器**，位于 `[7680,-768,8704,0]`；大部分4K窗口处于屏外，WGC仍可取得窗口表面。不能描述成“另一块物理4K屏实测”，不能拿该位置交给用户操作。两块物理4K屏实际都是150%。

尺寸证据：`capture/dpi-observations.json`、`display2-4k.png`、各 capture JSON。4K视频只裁窗口装饰，不做缩放。初始 startup `output=1280x720` 与 guest frontbuffer 字段均不能替代后续实际 swapchain 尺寸证据。

此前静态查看得到的线索（**未实测根因**）：

- `LostOdysseyRecomp/gpu/window_pixels.h` 的 scoped context 在窗口线程使用 PMv2，并设置 SDL Windows DPI scaling hint。
- `thirdparty/plume/plume_d3d12.cpp` 约1455行、`plume_vulkan.cpp` 约2474行的 `getWindowSize` 直接用 `GetClientRect`。
- 渲染线程的 DPI awareness/context 与 Win32 取尺寸返回值值得核对；未采集该线程真实上下文，不能先把猜想当修复依据。

全屏不填满与上述 DPI 异常是否同源尚不确定。Alt+Enter 是独立需求，可在确认窗口生命周期后与 resize／DPI 修复协调验证。不要把合理的宽高比黑边一概移除。

## 4. 独立 shader 日志的待办范围

用户原话：“另外把 shader log 独立出来。” 当前没有实现，未建立新日志 API 或修改输出路径。

已存在尚未实现的更完整设计：[独立 shader 日志与覆盖审计 TODO](taa-coverage-audit.md#shader-log-coverage-todo)，Project key `shader-log-coverage-audit`，应复用而非重新发明或建重复任务。该设计规划 `shader-*.jsonl`、与runtime同会话的最近三组保留、F1快照、按版本累计覆盖等；这些是规划，不是现成功能。本次明确的 urgent 交付是日志独立输出与本次定位需要的诊断能力，不自动把全部更广覆盖审计扩大为必须一次做完。

建议先检查实际日志入口和 shader／pipeline／cache 调用点，优先复用现有logging和F1打包路径。旧设计提醒：`command_processor::HashWords` 是word-FNV，`renderer::GetShader` 是byte-FNV；应标注namespace，不能直接将两类hash或dump文件名当作相同身份。`taa-coverage-audit.md` 的本地索引也指向已有跨场景shader清单和候选审阅，可用作同类风险审查起点；旧场景风险不等于这次Map16的已证实根因。

建议验收时关注：编译失败和 shader 身份不丢失；独立文件时间戳能与 runtime.log 对齐；一般运行日志保留必要失败摘要；F1反馈包包含需要的日志；多线程写入和退出flush可靠；默认不增加每draw同步落盘开销。只为有意义的行为做相称验证，不跑整个游戏回归套件。

## 5. Git、CI 与发布

交接时 live 查询：分支 `main`，HEAD `28be72f02649cf87126dd9f1a604ada6cdd380c5`。source version 为0.5.0，`0.5.0` 分支和 tag 已推送 GitHub。此轮文档交接没有 commit／push／tag／版本增量。

- GitHub remote 名称 **`github`**：`https://github.com/freefrank/LostOdysseyRecomp.git`。
- `origin` 是 Gitea；不要误推到另一处。
- [CI34362242667](https://github.com/freefrank/LostOdysseyRecomp/actions/runs/34362242667) 已成功，source/tag28be72f。
- [CI 交付清单](../../out/v0.5.0/release-finalization/ci-34362242667/CI-DELIVERY.json) 与 [核验报告](../../out/v0.5.0/release-finalization/ci-34362242667/REPORT.md)：50个manifest文件、身份、哈希、许可证通过，无新增功能重测。
- CI ZIP：44,020,136字节，SHA256 `e8391a2353a7206398b2dca24a2d73bccdea7b946d55cbfca4e7573648e95312`。
- CI EXE：`a1dd2d54bfbe1761eb8a31c4196e0f8ed6b63be4bc0dc8aa54de1d459bf97154`，与录像所用本地EXE不同。
- GitHub Release ID385591785，tag `v0.5.0`，`isDraft=true`、`publishedAt=null`、当前name为空；尚无已验证的匿名公开下载。
- 旧本地包：44,080,866字节，SHA256 `100e6491548579e3a13aa60564bc760bca121113915842f73817d1b943482ce4`，source86ba2c1。它是当前录像／用户运行基线，不是最终CI包。

首次 CI34359828016 失败原因仅为 DXC 许可证上游 CRLF 被 checkout 转LF，28be72f 增加精确 `.gitattributes` 修复，成功CI已完成。不要重开这个已解决问题。旧tag保存在 `refs/backup/v0.5.0-ci-34359828016`，无需为文档改动移动tag。

发布前的新源代码修复需要对应产物和必要验证；现有CI仅证明28be72f。不要把未来修复写进旧包的来源或验证记录。恢复发布时先核对live tag／Release状态，避免重复发包；单纯docs、换目录或重新打包不构成功能重测理由。

## 6. 工作区与接续文件

交接前已有未提交文档：`.github/ISSUE_TEMPLATE/bug_report.md`、双语README、`docs/INSTALLING.md`、`docs/RELEASE-v0.5.0.md`、双语ROADMAP、`docs/STATUS.md`、`docs/notes/release-packaging.md`、`docs/notes/v0.5.0-qol-requirements.md`、`docs/project-management/items.json`、`sync-state.json`。本handoff也是新增未提交文件。

`thirdparty/plume` 和 `tools/XenonRecomp` 的 dirty 状态是预期受管补丁，不要清掉或无意stage。不要 reset 工作区、覆盖存档或清理保留的诊断基线。

曾出现同步工具导致 `.git` 状态回退，已做隔离和备份；备份在 `C:\Users\freefrank\.codex\scratch\lo-v050-git-state-20260909-1310\`。接续时先核对真实branch／HEAD，不依赖旧 hook 注入的 Issue12历史计划。`task_plan.md` 最新开头应指向本文。

Project：[Roadmap #3](https://github.com/users/freefrank/projects/3)。`project_manager` 本轮同步六项 Urgent／v0.5.0 和发布／Reddit暂停；以 `items.json`／`sync-state.json` 最终readback为准。已有DPI、全屏、Alt+Enter项复用，不重复新建Issue。`docs_sync` 负责纠正当前CI证据路径、AA Off事实和发布前urgent范围，主代理负责本handoff。

本轮同步已完成：新增性能与4K阴影两个draft，复用另外四项；最终正文纠错同步0新增／6更新／163不变／0冲突，回读169项无待写入和冲突。六项均为Todo／Not started／v0.5.0，P0在Evidence中明确保存。两agent均已完成交接任务，后续不会自行继续实现或测试。

之前 `capture/run-01/shader-cache` 删除被自动审批以 `blocked by policy` 拒绝，已原样保留，未绕过。该缓存也被后续隔离运行复用；不要把它误认作可随意删除的无用文件。

## 7. Reddit 资产与复用边界

目录：`out/v0.5.0/reddit-launch/`。

- `POST.en.md`／`POST.zh-CN.md`：r/recomps草稿，没有发帖。
- `FEEDBACK.en.md`／`FEEDBACK.zh-CN.md`：简短异常描述＋完整log；画面问题加F1 Capture render state ZIP。不再要求手填log已有的显卡、驱动、后端和场景信息。
- `EDITOR-NOTES.md`、`PREVIEW.html`、`LostOdysseyRecomp-v0.5.0-Reddit-kit.zip`：待修复与新媒体后更新，旧预览／ZIP可能滞后于当前暂停决定。
- `media/07-debug-menu.png` 是实际Map16运行的原生Debug Menu截图，已交付；`03-settings-english.png` 已换为原生4K设置截图。
- `media/lost-odyssey-v0.5.0-demo.mp4` 仍为旧42秒720p成片。**真正4K＋TAA宣传视频未完成**；故障诊断片不能代替正常展示。
- `media/manifest.json` 保留每项真实版本／来源，部分图片是历史0.4.x，不要全部标成新CI包截图。恢复后再生成预览和ZIP，不必为文档重复游戏测试。

文案重点保留：0.5.0 milestone、100+累计commit、最近三天和2B+tokens（后两者是用户统计，不能声称100+commit全发生在三天）、CPU优化、Vulkan、画质与QOL；未来bugfix、shader优化、DLSS／FSR／XeSS、FG、DX11、原生Linux/macOS，以及与u/Adoky**实验合作**Switch port。

CPU宣传图复用原有Map2固定场景数据：0.4.15至0.4.16，整机归一化进程CPU15.6200%→4.30336%，约下降72.4%，两者59.983 presents/s；证据 `out/v0.5.0/cpu-optimization/report/REPORT.md`。不是这次4K性能结果，也不是新的CI／Vulkan benchmark。

既有设置菜单、GC Issue12、DLC导入、Xenia存档兼容和限定D3D12／Vulkan验收仍可复用；新的Map16／4K问题不等于撤销所有旧结论，也不能被旧结论覆盖。Issue12历史早期“未定位／未修复”hook已经过时，接续本任务不要重跑花朵存档调查。

## 8. Subagent 派发与协作规则

以下复用仓库 `AGENTS.md`、[文档同步规则](../agents/documentation.md)、[项目管理规则](../agents/project-management.md)，不是另设定时任务。新 session 先读实际规则；与本文冲突时按用户最新指令和当时生效的上级规则执行。

### 固定角色

| 角色 | 当前配置与触发 | 文件／职责归属 |
| --- | --- | --- |
| 主代理 | 统筹计划、拆分独立工作、审阅结果和协调构建／运行 | 共享状态、证据取舍、源码集成、最终交付；不替各worker重跑已通过检查 |
| `project_manager` | `gpt-5.6-terra`／`medium`；需求新增或变更、开发开始／暂停／结束、验证或验收变化、提交／发布状态变化、显式Project／TODO同步时按需调用 | GitHub Project #3、`docs/project-management/items.json`／`sync-state.json`、双语ROADMAP的任务状态与优先级 |
| `docs_sync` | `gpt-5.6-luna`／`medium`；功能／修复验收后，提交／发布前后有行为、验证或发布事实变化，以及显式文档同步请求时按需调用 | README语言对、CHANGELOG、STATUS、release notes和明确分配的技术记录；纯笔误可主代理直接修 |
| 实现／诊断worker | 独立且范围明确的代码、诊断或验证任务；按当前可用模型和明确的用户选择派发 | 主代理指定的源码／报告目录和验证责任，不自行扩展到相邻模块或另开大量workflow |

账号配置文件位于 `C:\Users\freefrank\.codex\agents\docs_sync.toml` 与 `project_manager.toml`；若设置了 `CODEX_HOME`，以其 `agents/` 目录为准。本轮已读配置确认上述模型和reasoning。优先选择对应自定义agent type；不可用时读取定义并创建同名普通subagent，显式使用配置的模型／reasoning和有界交接。普通实现worker默认继承，不擅自沿用已纠正的Opus请求；用户先前的“Opus”已纠正为Sol，Astra曾明确用于Vulkan实现。

同一session内，相关后续任务可复用现有agent；只在有可并行的独立工作时增加worker。新session不能假定旧session的agent句柄仍可使用，应从本handoff恢复并按需新派。已完成agent不继续挂着跑任务，不需要为交接启动常驻服务或创建用户侧新任务。没有可用subagent／达到限制时，由主代理依相同流程完成必要本地同步并简短说明，不无限重试派发。

### 每次派发必须携带

1. **目标和边界：**本次具体结果、明确负责的文件／模块、允许的动作、停止条件。本轮为handoff；恢复实现后六项全部P0／v0.5.0，禁止迁移到v0.5.1。
2. **证据：**实际commit／EXE／日志或报告路径、已完成检查及其覆盖边界、尚未验证之处、用户原话中的验收／优先级、当前发布状态。
3. **协作说明：**“你不是唯一writer；保留其他人修改，不要revert他人工作，必要时适配其改动。” 共享文件只安排一个writer，其他人先提供变更点。
4. **验证限制：**明确已经通过且禁止重复的测试；新的改动只跑直接受影响的最小验证。不要为文档、版本号、提交、tag、换目录或打包重复整套验证。
5. **回报要求：**实际改动、证据和检查结果、剩余未知、产物路径、是否仍有自己启动的进程。完成后停止所属任务，交主代理review。

`docs_sync` 与 `project_manager` 不递归委派，不构建或操作游戏，不改源代码／版本，不自行commit、push、tag、发布或发送Issue评论。主代理在其工作期间继续独立工作，收尾读实际diff；agent的完成报告不是免审证明。

### 文档与Project的分工

- 双语ROADMAP由 `project_manager` 独占写入，`docs_sync` 只同步主代理明确交出的段落，不能同时改共享mirror。README英文／简体中文保持一致，STATUS使用英文，CHANGELOG保留双语惯例。
- Project是任务状态入口，ROADMAP是仓库镜像。`docs_sync` 保留实现、测试、用户验收、发布的事实叙述，双方共享精确证据，不能用Issue关闭或CI成功代替验收。
- 项目绑定读取 `docs/project-management/project.json`；先匹配已有Issue／稳定key，再使用Project draft，避免重复。用户已授权本仓库Project同步，不需要每项另问许可；这不授权创建无必要Issue、改Issue状态或发送评论。
- 本轮Project没有Priority字段；六项用 `Evidence` 的 **P0 urgent v0.5.0 pre-release requirement** 明确标记，`Release=v0.5.0`，`Status=Todo`／`Delivery=Not started`。不要虚构已存在的Priority选项或擅改Project结构。
- `tools/project_management/sync.py` 默认生成计划，加 `--apply` 才写GitHub。更新后读回实际受影响字段；正文也要检查，防止“Future／不影响发布／尚无复现”等旧表述与新字段矛盾。同步器未处理draft正文时，不把仅字段一致声称为正文也已同步。
- 沿用已有通过的Project readback，只针对实际后续变更验证，不反复全量审计历史任务。缺失证据就保留pending，并交主代理裁决。

### 构建、游戏和版本的统一调度

- 每个需要实景验证的阶段只指定一个游戏操作owner，说明准确EXE、隔离目录、场景和输入／结束条件。主代理不同时发送另一组输入。
- 默认后台、不抢焦点、测试静音；前台交给用户需有当次明确安排。保留原始配置和存档，实验shader cache按需要隔离。
- 独立静态审查／代码编辑可并行；避免多个agent同时全量build、CI或游戏，把电脑CPU占满。需要构建时先核查已存在的build／workflow，复用可用输入和既有证据。
- 主代理集中处理版本号和Git状态。历史规则是每个完整feature实现并通过必要验证后 `+0.0.1`，规划、文档、子任务或重复测试不加版本；当前已经有v0.5.0草稿与tag，新源码修复的版本／tag／包必须保持真实对应，绝不因patch增长改变用户的v0.5.0目标。
- 发布当前暂停；不要依据旧“已授权发布”的阶段记录抢先公开或重发旧包。完成本次urgent内容后，由主代理按最新发布安排处理source/tag/CI/产物与文档，已通过的无关功能验证直接复用。

## 建议的接续顺序

1. 读本文和 `capture/REPORT-4K.md`，核对当前Git与进程；先读两次运行日志，不立即开新游戏。
2. 明确所诊断的是哪次运行／时间段／AA设置，保存基线，定位黑地面实际资源和shader路径；现有日志不足时再设计最小补充捕获。
3. 并行工作可分为渲染错误诊断、独立shader日志、窗口／DPI／Alt+Enter；主代理整合，限制同时构建／跑游戏数量，避免再次占满电脑。
4. 性能先做必要的真实测量，再定优化方向；不把TAA视频容器或heartbeat当benchmark。
5. 每项修复只补受影响的必要验证，再同步docs／Project，保留实现、实测、用户验收、发布四个状态。
6. 问题处理完成后更新4K＋TAA视频、截图、帖子和反馈材料，再恢复发布流程。

默认 agent 实景测试后台、不抢焦点，非音频任务测试静音，原存档／配置保留。此前一次前台授权仅用于用户亲自观察，不等于允许后续测试持续抢前台。用户将在新session继续，本轮到交接为止。

## 2026-09-09 当前实现接续

本轮候选构建已包含 Map16 temporal constant 修复、独立 shader JSONL 日志、accepted-present/frame timing、GPU batch timing、bulk register snapshot，以及 DPI 感知的物理像素窗口、显示器切换位置恢复和 session-only Alt+Enter 状态。最终 source-0.5.6 版本消费者构建的 EXE SHA256 为 `d582250b1a343f83da272436c3acead0daeeb8035129df5ae79962f47dd41752`，证据见 `out/v0.5.0/rendering-fixes/build/version-execution-result.json`；功能验证继续绑定 earlier candidate EXE `9a7b626320ede0bd82bcbf93cde08aaeefc2c631aa29ebacba4fc31a5d321288`，版本消费者构建没有追加功能重测。

定向证据包括 20,635 项 temporal CPU 检查、日志/DXC/归档检查、修正后的 150% DPI 窗口 fixture、18,442 项寄存器快照检查，以及现有 render timing fixture。Map16 固定场景实跑已覆盖 32 个连续 3840×2160 phase，地面正常、summary 全部 ready/completed/history-reused、无 gap/jitter miss，160 个材质上传与对应深度上传逐位一致；DPI144 运行记录 raw 2560×1440、physical 与 swapchain 3840×2160，截图地面正常；Vulkan DPI144 尚未运行。内部帧时序的 bulk 与 legacy 均覆盖完整 30 秒，分别记录 1,419 和 1,255 个连续帧。外部 CPU/NVML 补充采样覆盖 bulk 完整 30 秒，legacy 仅覆盖其中 16.13 秒。前 16.13 秒的系统 CPU/GPU 负载和 GPU clock 也不同，因此不能把全部 accepted-present 差异归因于常量修复；同一 EXE 的直接常量段从 4.147 ms 降至 0.576 ms，观测到的 accepted-present interval 23.914→21.143 ms 仅限本次 A/B。它不是稳定 60 FPS 或全游戏 benchmark。用户验收、用户全屏和 Alt+Enter 验收仍未完成。`LO_BACKGROUND` 强制 Windowed。RTSS SDK 已接入，但本次游戏运行没有有效 RTSS hook 帧数据。敌人 `c230` 路径和更广 family coverage 仍是后续工作。

## 2026-09-09 性能诊断当前澄清

同一 source-0.5.6 EXE、Map16 固定视角和 4K 输出下，独立 RTSS 采集已恢复：窗口需要 `WS_VISIBLE`，但保持 `SW_SHOWNOACTIVATE`、`HWND_BOTTOM` 和不抢前台。18.391 秒窗口得到 18 个完整统计窗口内的 883 次 Present，48.0126 FPS；观测 frame time 均值 20.8115 ms、p95 22.890 ms。先前隐藏运行的 RTSS 全零是 RTSS hook 的 `IsWindowVisible` 过滤，不是游戏没有 Present。RTSS 数据是拦截到的 Present 统计，不代表物理 scanout。

同一运行的内部 accepted-present 记录为 20 秒、961 帧、mean 20.8071 ms、p95 22.8661 ms、p99 26.0708 ms、48.0606 accepted/s。采样 profiler 显示 renderer/command thread 69900 使用约 57.62% 单逻辑核，448 个样本中 114 个直接位于 wait，88 个调用栈到达 `Renderer::Flush`；另外可见重复 shader FNV、vertex `SampleHash` 和 index conversion。wall 样本比例不是 CPU 利用率，也不能直接换算可回收毫秒。

目前只确认诊断边界，没有实施新的性能优化。CPU/GPU 每个 batch 的 execute 与 wait 仍串行；500 是应用 descriptor heap 的 batch 预算，不是硬件上限。用户的 9800X3D + RTX 5080 在该固定场景仍未达到稳定 60 FPS，最终 1080 控制、后续优化和用户验收保持待办。详细证据见新增的[性能诊断记录](v0.5.0-performance-diagnosis-2026-09-09.md)。

源码仍为 0.5.0，候选未提交、未发布，Release 和 Reddit 继续暂停。不要把 focused fixture、RTSS 接入或候选构建写成用户验收或发布包验证。

## 后续直接修复 — source 0.5.7

用户要求停止追加诊断并停用 subagent 后，主代理直接修复了重复 shader FNV 热点：复用命令处理器的 word hash 作为索引，完整比对微码内容后缓存 renderer byte hash，供 shader 与 pipeline 查询共用。同地址内容改变及 hash 索引碰撞仍会校验，CPU 缓存设有容量边界。74 项定向检查通过，修复候选完成一次 20 秒 Map16 对照。

保持 D3D12、TAA3、60 FPS cap 和 3840×2160 内部/输出，同一存档固定视角：内部帧间隔 20.8071→18.5178 ms，48.0606→54.0022 accepted presents/s；RTSS 48.0126→54.0511 FPS（观察提升 12.6%），p95 22.890→19.995 ms。绘制 wall time 13.0326→10.7875 ms，GPU 批次 4.8042→4.7204 ms。新窗口 1080 连续帧，无新增 shader/pipeline/texture、capture 或 trace；截图场景正常。系统背景 CPU 和 GPU boost 略有差异，这是限定条件下的观察结果，尚未达到稳定 4K60。

验证后版本递增到 0.5.7，四个版本消费者重编并链接，未重复游戏/测试。游戏正常关闭，存档、profile 和任务配置未变。尚未 commit/push/release；完整结果与可执行文件见 `out/v0.5.0/performance-fix/REPORT.md`。后续热点仍包括 vertex/index 准备与按批次同步等待，用户验收待完成。

## 当前 0.5.0：几何准备与限帧等待修复

根据用户最新要求，源码和发布目标统一恢复到 **0.5.0**，不再按功能递增；AGENTS.md 已同步。旧 0.5.6/0.5.7 文件名仅保留为历史产物证据。本轮未使用 subagent。

顶点缓存改为精确比较原有采样范围内的字节，索引／图元转换复用临时数组并将宽度、字节序判断移至循环外。内容变化仍分配新 GPU 区间，不覆盖在用资源。针对实测普通休眠的唤醒超时，限帧改用可复用的高精度等待定时器；原有 FramePacer 规则不变，不采用忙等。几何值／边界检查以及高精度、普通定时器两种模式的截止时间检查通过。

固定 Map16、D3D12、TAA3、内部和输出均 3840×2160：RTSS **54.05→58.33→59.76 FPS**；最终内部平均 **59.79 FPS / 16.72 ms**，p95 **17.16 ms**。相对 shader-only 基线，顶点准备 **1.57→0.65 ms**，索引准备 **1.93→0.29 ms**；几何版至最终版的平均唤醒超时约 **0.65→0.28 ms**。最终窗口 1196 连续有效帧，截图场景正常，窗口/DPI 不变、未抢前台，无新增 shader/pipeline/texture 或测窗内 capture/trace。系统 CPU、GPU 频率及采集器开销不同，属于限定实景的观察改善，不能代表全游戏稳定 60 FPS。

最终实测 EXE：`out/v0.5.0/performance-fix/final-0.5.0/LostOdysseyRecomp/LostOdysseyRecomp.exe`，SHA256 `394bbb847f9fe48342800b270dee05fd4ac7bbcc56e1b457f6d922e80a772358`。游戏均正常关闭，存档/profile/配置未变。未提交、推送或发布；详见 `out/v0.5.0/performance-fix/FINAL-REPORT.md`。


## Vulkan startup screen follow-up
User package SHA256 394bbb847f9fe48342800b270dee05fd4ac7bbcc56e1b457f6d922e80a772358 reproduced a stale Preparing pipelines screen with audio. Runtime log 1788976522589370 reports 20/20 pipelines ready in 10 ms, followed by guest swap heartbeats; these heartbeats alone do not prove host presentation. Source review found missing physical DPI scopes around Vulkan acquire/present and no OUT_OF_DATE recreation when dimensions are unchanged. Both are repaired. Candidate source 0.5.0 SHA256 92b49272e389f5e42c28a334fbefbd0970a72a0d806f42ebe87d47210baf798a, out/v0.5.0/performance-fix/vulkan-startup-fix/LostOdysseyRecomp/LostOdysseyRecomp.exe. One Plume source compiled and linked successfully; user explicitly requested no validation and will test. No runtime test, deployment or publication performed. Root handled documentation locally with subagents disabled.


## 2026-09-09 TAA collection deployment

Source/release target0.5.0. Client first-setup/existing-settings opt-in, persistent opt-out, bounded upload and10 confirmed vertex projection fixes built. SHA256 38af5ec6006c77dce59ab3f84bc20d730d4dd448a133c3a4148874dd9e9c66b3. Artifact: out/v0.5.0/performance-fix/taa-collection-0.5.0/LostOdysseyRecomp/LostOdysseyRecomp.exe. Worker/D1 live at https://lo.dotslash.pro/v1/taa; deployment7bbbd436-2ede-4ec8-a132-f84d631d4922. Three Worker tests passed; health200; three concurrent duplicate probes produced one stored row, probe removed. No game launched, player consent UI/visual acceptance pending. Local docs maintained by root; user authorized only vertex-fix subagent. No Git commit/push/release. MV/DLSS-G collection remains future work, not included in consent/schema1.


### 0.5.0 settings-entry crash follow-up

The TAA collection candidate 38af5ec6 crashed while opening settings: mixed old/new translation table COMDAT definitions caused an out-of-bounds lookup. Internal linkage and recompilation of all four translation consumers address the mismatch. Replacement 06516642 compiled and linked; gameplay validation remains with the user. Evidence: `out/v0.5.0/performance-fix/taa-settings-crash-fix-0.5.0/REPORT.md`. No publication.


### Sparse temporal collection — 0.5.0

Client `bad75141` and Worker `2191fd5e-42ef-426c-8ac3-9ad3c1ae28c7` add 32×18×32 sparse depth/camera-only MV with jitter/matrices, bounded compression and authenticated reception summaries. Reuses existing GPU fence; one pending group, minimum five-minute collection interval. C++/Worker roundtrip and shader compilation passed; HTTPS probe deduplication passed and synthetic row removed. User game test and real receipt remain pending. Existing internal consent retained; new prompt describes expanded data. Full object/skinned MV and DLSS generation remain unimplemented. Evidence: `out/v0.5.0/performance-fix/temporal-collection-0.5.0/REPORT.md`. No publication.


### Shader anomaly priority and four c7 paths — 0.5.0

Candidate `1c9911a3` covers four confirmed c7 paths (48 draws/frame in capture 2813–2815). Shader anomalies now precede routine summaries and MV archival traffic, with 10-second urgent scheduling, dimension-variant caps and reserved queue capacity. Renderer/collector build and link succeeded; capture evidence reused, no game run. User visual acceptance and live upload-priority verification pending. Worker unchanged; shader build ID `0.5.0-shader-priority-1`. Evidence: `out/v0.5.0/performance-fix/shader-priority-0.5.0/REPORT.md`. No publication.

### Current acceptance — four c7 paths

The user accepted the four capture-confirmed c7 path repairs in candidate `1c9911a3` for the Sol scene. The check contains six spaced presented-frame screenshots over approximately 11.37 seconds; the observed samples showed no flicker. This resolves the tested same-scene coverage gap. It does not claim a continuous 11-second recording, whole-game repair or broader scene coverage; those remain regression work. Evidence: `out/v0.5.0/slot2-sol-check/REPORT.md`.
