# Original Settings menu assets

The Settings renderer now loads the selected installed `LO.fpi` language package at runtime and uses its native `Maru23` font data and `UI_MAIN_00` grey panel texture. An optional same-package `Abc` face supplies whole-string fallback for the multiplication sign used by resolution labels. Missing packages, unsupported formats and uncovered glyphs retain the existing GDI fallback.

The bounded native evidence is recorded in [`INTEGRATION-REPORT.md`](../../out/v0.5.0/final-preparation/menu-assets/INTEGRATION-REPORT.md) and [`ABC-CHECKPOINT-MANIFEST.json`](../../out/v0.5.0/final-preparation/menu-assets/ABC-CHECKPOINT-MANIFEST.json). Three independent decoded-output comparisons matched the retained proof. The run-06 Graphics frame is historical source-0.4.22 evidence: 41 original-font draws and 2 GDI draws. Source 0.4.23 uses `反走样` and `画面速率` from existing original glyphs.

This source-0.4.22 asset integration preserved menu geometry, input, save/restart transactions and presentation behavior. Source 0.4.23 changes save/restart and guest return UX. It adds no bundled game assets, Python runtime or extra runtime DLL; the static MIT `lzokay` dependency is included with its license. Direct checks cover the selected package and bounded English/Simplified Chinese paths. The historical 0.4.22 package remains linked in its own report. The user accepted the overall layout/style direction shown by run-06 `graphics-abc.png`; no new user feedback has been received on the new glyphs. Graphics settings save/apply on one click; restart-required changes ask only Now/Later after a successful save. The bounded replacement-flow fixture and one hidden Windowed D3D12 runtime path passed; the custom menu closes through the original guest apply/close path without actively opening the original confirmation dialog. Broader GPU/DPI coverage remains separate.
