# v0.5.0 QOL 需求与未来验收标准

## 2026-09-08：本轮细化与发布范围

安装器新增无边框窗口与交互改进已完成，记为本地源码 0.4.16：9 项直接 UI 检查、2 项受影响的控制器检查通过，已审阅正常／最小尺寸实绘。更新器改进已完成，记为本地源码 0.4.17：专项 fixture 通过，并已审阅正常、未知总量和窄中文实绘。Debug Menu 改进已完成，记为本地源码 0.4.18：专项 fixture 通过，150% DPI 布局修复已纳入验证范围。三处界面均按最新要求减少常驻说明文字，保留操作名称、必要状态和错误。下方 Done 记录保留此前基线范围；本轮只补直接受影响的检查，复用已有功能证据。详见[本轮记录](desktop-ui-modernization.md)。

用户已验收 CPU Vulkan 对照和 Issue #9，后者已关闭；不据此补造 Vulkan 对照指标或原报告者确认。DX11 不是 v0.5.0 的承诺，跨 GPU 覆盖等待用户反馈。三组真实 DLC 首次与重复导入通过；历史程序绑定三根并部分读取后发生 guest 0x1A 访问异常，未打开迷宫主体，未验收奖励／迷宫玩法。0.4.xx 保持内部开发编号，积累至 v0.5.0 一起发布。

本文记录 v0.5.0 规划中的 13 项 Windows QOL 需求，并保留各项需求与验收标准。当前未发布源码为 0.4.23；QOL10 的原版菜单素材、字体标签与 UX 已完成限定实现和验证，用户此前接受整体布局／风格方向，当前没有针对新字形的用户反馈。QOL13 的生产编译与窗口集成检查已通过。当前发布基线仍为 v0.4.2，证据与边界见[项目状态](../STATUS.md)及 `out/v0.5.0/` 报告.

交付状态：当前 0.4.23 源码包含已完成生产路径验证的 Issue #12 GC 修复、DLC 目录过滤运行验证，以及 QOL10 菜单素材与 UX 的限定验证；QOL13 的完成记录保留 0.4.19，后续源码版本按实际 feature 增量递进。DPI/DLC/菜单素材/UX 源码及版本记录仍为本地未发布内容；GC 实现已以 `17e3ab7` 合并并包含在 GitHub 备份中；0.4.22 与 0.4.23 开发包保持各自历史身份，公开发布仍待进行。玩家对新字形暂无新反馈，未增加额外发布门槛.

当前完成项：QOL1 保留 ISO/GOD/解包文件夹/独立 XEX 读取和导入前识别复核，限定导入检查及 15 文件合成导入流程通过；只接受已审计 XEX SHA256，未知哈希仅作诊断。QOL2 空 `game-path.txt` 使用 EXE 旁的 `../game`。QOL4 直接启动发现根目录 `default.xex` 或配置／导入根下的 `disc1`，并按记录回退到 `game/disc1`、`game`、EXE 及 EXE 上级目录的 `game`；显式非空 `--game` 优先且保持隔离，坏的显式配置不静默回退。完整游戏、全 DPI 矩阵和完整通关仍未验证。

## 2026-09-09：QOL13 与 DLC 当前边界

QOL13 的源码实现已稳定在 0.4.19 工作线上：video window 在线程内覆盖完整 init、event 和 destruction 生命周期；`window_pixels.h` 在初始化前固定 `SDL_WINDOWS_DPI_SCALING=0`，结束时恢复线程 awareness；auxiliary UI 行为不变。首个实际 fixture 运行退出码为 0，前 3 次仅为修正 SDL intrinsics 编译兼容性的准备。已验证一台 96 DPI／100% 桌面上的 640×360 与 1280×720 客户区物理像素等于 SDL size/drawable，独立 unaware 线程 `GetClientRect` 结果一致；两次完整生命周期及空 scope 恢复通过，SDL 冲突 scaling=1 也覆盖。96/120/144/192 的 `WM_GETDPISCALEDSIZE` 消息契约通过，生产编译和窗口集成检查也已通过；125/150/200 仍不是实际桌面或跨屏覆盖。本项已完成必要验证，但未发布 Release。证据：`out/v0.5.0/window-pixels/run-01/{REPORT.md,manifest.json}`、`out/issue12-triage/production-validation/{build/execution-result.json,window-run15.json}`。

DLC 的三份真实包首次导入及重复选择／保留通过；目录过滤修复后的 runtime02 已分别读取 XDC0/1/2 中的 LODLC002/001/003，三个包均完成 header、完整索引和 payload 读取，合计 24 次，spa.bin 零匹配、零崩溃，主菜单正常。13 个导入文件及隔离 save/profile/settings 未变；奖励和地下城玩法仍未验收。此前的 exact-EXE pattern 诊断与 Xenia 语义说明保留为历史依据，不代表当前仍在修改或等待该修复；当前 runtime02 结果已通过。

用户已确认当前 D3D12/Vulkan 场景可用；Windows Vulkan 的既有证据仍限于 RTX 5080 的 Maps 2、3、12，且 Map2/3 与 Map12 使用不同冻结候选及证据范围，不能合并为同一 binary 结论。这不能替代完整游戏、其他 GPU、完整视觉或发布验收。后端选择的能力、失败回退和设备探针专项检查已通过；此前另有 v0.4.19 runtime build candidate 作为未部署的历史本地候选，当前 0.4.18 合并构建与打包已有报告记录；该历史候选与当前 0.4.19 QOL13 源码改动及其冻结 0.4.18 测试 EXE 身份分开。DX11 仍不支持运行时。性能记录、菜单、更新器和捕获证据仍按各项边界分别计算。

## 当前状态（2026-09-08）

当前未发布源码为 0.4.23；QOL10 的整体布局／风格方向已获用户接受，0.4.23 字体标签、单击保存／应用、Now/Later 和 guest apply/close 已通过限定 fixture 与一次隐藏 Windowed D3D12 路径；更广 GPU/DPI 和失败／重启实际分支未覆盖，新字形尚无新的用户反馈。QOL12 是 updater 要求，Issue #12 GC 生产验证独立记录；QOL13 fixture、生产编译和窗口集成检查均已完成。

本表保留上一轮限定验证；QOL1、QOL8、QOL12 的新增 UI 细化状态以上方本轮说明与路线图为准。

| QOL | 状态与边界 |
| --- | --- |
| 1 | Done + Validated (bounded)：安装器识别／导入检查及 15 文件合成流程通过；未知 XEX 哈希仅作诊断。 |
| 2 | Done + Validated (bounded)：空 `game-path.txt` 使用 EXE 旁 `../game`。 |
| 3 | Done + Validated (bounded)：ICO 七尺寸、PE/game-helper 资源和实际 SDL window resource match 已通过并接受。 |
| 4 | Done + Validated (bounded)：根 `default.xex`／import root 下 `disc1` 及记录的目录回退通过；不代表完整游戏启动。 |
| 5 | Done + Validated (bounded)：真实 `IFileOpenDialog` 文件夹选择器通过 long Unicode 路径身份、取消和 COM 行为 gate。 |
| 6 | Done + Validated (bounded)：v0.4.17 在原安装目录完成两次 `prepare-only`；第二次复用 22,972 ready entries、0 source read/translation/actual DXC，PSO 288 ready。两项既有 shader failure 仍可见；用户启动验收、全游戏与其他平台仍未确认。 |
| 7 | Done + Validated (bounded)：15 workers / 16 logical CPUs；22,955 modules ready、0 failed；仅覆盖当前启动样本。 |
| 8 | Done + Validated (bounded)：child-focus F1/Escape/Gamepad B、busy/error feedback 和真实 Map2 Debug 窗口／地图坐标通过；物理 F1 送达仍受 Sky `SendInput` error 87 限制。 |
| 9 | Implemented + bounded validation：关闭自制 Settings 菜单改走原 guest apply/close 路径，不主动建立原版确认对话框；一次隐藏 Windowed D3D12 运行路径通过，更广 guest 场景仍未验证。 |
| 10 | Implemented + bounded validation：原版 Maru23/UI_MAIN_00 及同包 Abc fallback 已接入。0.4.22 菜单素材子改动未改变几何／输入；0.4.23 改变图形保存／重启与 guest 返回 UX。Fixture 与一次隐藏 Windowed D3D12 路径通过；更广语言、GPU、DPI 覆盖仍未验证，新字形尚无新的用户反馈。 |
| 11 | Done + Validated (bounded)：Restart now/Cancel 和父子进程 handshake 在记录边界内通过；无正常子进程退出证明。 |
| 12 | Done + Validated (bounded)：core/helper/progress/main-hook、main metadata 和最终本地开发包 gate 通过；开发包跳过正式自动更新，47 个清单文件逐 SHA 校验通过。 |

Map12 记录包含 TAA 253/256 ready/completed/reused、三次 capture stall 恢复、jitter misses 0 和三帧 raw 保存；生产 host 归档完成 273 个 entry，ZIP SHA256 为 `12ee6d8744627a161a9455edcda85e53b7c085d0b4b6eb1d37b77f5fedb3626d`。该归档完成由 host 侧启动流程证明，不等同于原 game worker 自然退出；没有再次启动 Map12。DX11、其他 GPU 和全游戏覆盖仍属后续回归范围。

最终本地开发包为 `LostOdysseyRecomp-windows-x64-v0.4.15-147bffb2-dev.zip`，43,741,228 bytes，SHA256 `af4f078a72187795def2984eef9b8faff7f776d517a7b2310886cd5aba1c3996`；`development_build=true`、formal version 为空、source version 为 `0.4.15`，包含严格 allowlist、runtime/helper、三份 EXE 图标资源、DXC 2407 pair/licenses，且不含 guest/source/PDB/user data。证据：`out/v0.5.0/package/{package-validation.json,manifest.json}`。

## QOL13 — 游戏主窗口按物理像素分辨率显示（Implemented; bounded fixture validated）

游戏主窗口的 client/presentation 尺寸应按用户选定的实际物理像素分辨率确定；100%、125%、150% 或 200% 桌面缩放不应额外放大游戏主界面。Config、Installer、Updater 和 Debug Menu 继续适配桌面 DPI。当前实现、fixture、生产编译和窗口集成已通过限定范围；125/150/200 实际桌面及跨屏覆盖仍未验证。本项已在 0.4.19 完成 patch 增量；尚未发布 Release。

## 需求清单

1. **Installer 适配安装来源与目录结构**

   Installer 应能处理不同文件夹结构、ISO、已解包文件夹和单独 XEX，并可选用 MD5/SHA256 做版本识别；界面需要清晰、美观并说明识别结果。未来验收至少覆盖这些输入的成功、版本不匹配、缺失文件和取消/重试路径；错误应提供可恢复提示。路径搜索以显式用户选择优先，并覆盖当前 EXE 目录与空路径的 `../game` 候选。

2. **`game-path.txt` 默认路径**

   当 `game-path.txt` 为空时，默认使用相对于当前 EXE 目录的 `../game`。显式配置仍优先；未来验收需覆盖空文件、有效显式路径、相对路径解析和无效路径提示，并确认不会误读工作目录之外的旧配置。

3. **Recomp EXE 图标**

   `LostOdysseyRecomp.exe` 必须使用突出 Recomp 身份的 Windows 图标；可以采用原游戏图标改造的方向，但不是硬性实现方式。未来验收需检查 Explorer、快捷方式、任务栏和安装包中显示的图标与架构资源是否一致。

4. **EXE 直接启动与资源定位**

   用户应能直接运行 `LostOdysseyRecomp.exe`，无需旁边的 `.bat` 或 `.cmd`；资源默认从当前 EXE 目录定位，并遵守显式路径优先规则。未来验收需覆盖从资源目录、快捷方式和不同工作目录直接启动，以及缺失资源时的可恢复错误提示。

5. **独立、现代化的首次启动配置界面**

   首次启动配置菜单应从当前流程中独立为现代化界面/模块，便于选择语言、资源路径和初始图形设置。这里的“独立”指界面与模块边界，不要求额外 EXE。未来验收需确认首次启动、取消/返回、重复启动和配置保存路径之间的行为一致。

6. **区分启动时 shader 准备阶段**

   用户反馈每次启动仍显示 `compiling shader cache`。目标是避免不必要的重复编译：有效缓存应复用，只编译缺失或失效的项目。后续工作必须先区分缓存读取、资源发现、PSO 预热、实际 shader 重编译和仅显示旧提示的情况；`Compiling` 字样本身不构成全量重编译实证。未来验收需用日志或等价可观察证据说明每个阶段的实际发生次数，并覆盖缓存已存在与缓存失效两种启动。

7. **shader 编译线程数**

   编译线程数规则为逻辑线程数减一，最小为 1。该规则已有历史实现记录；后续应核实当前启动路径是否完整覆盖，必要时修复并进行相称验证。未来验收需覆盖单线程、双线程及更多逻辑线程环境，并确认不会创建 0 或负数线程。

8. **轻量、现代化的 Debug Menu**

   Debug Menu 应保留现有诊断能力，同时采用轻量、现代化的布局和交互，避免阻塞正常游戏操作。未来验收需覆盖打开/关闭、键盘与手柄焦点、滚动/缩放、状态读取以及现有捕获和诊断入口；诊断失败应仍可返回游戏。

9. **以项目设置菜单替换内置菜单的可行性**

   评估在可行时用项目设置菜单替换内置菜单。该项先经过可行性门槛：资源/输入/保存契约、原版流程依赖、兼容性和回退路径必须有证据；未通过门槛不得宣称已替换。未来验收需保留安全回退，并覆盖设置保存、取消和返回游戏。

10. **设置菜单忠实复刻原版风格**

   设置菜单以忠实、完整复刻原版游戏风格为目标。它与第 5 项的现代化首次启动布局是不同目标：首次启动强调清晰配置，设置菜单强调原版呈现与行为；实际可达程度由未来视觉验收确认。未来验收需分别比较布局、字体、层级、选中态、确认/取消和输入行为，不以现代化首启界面代替原版风格验收。

11. **需要重启的设置提示**

   对需要重启才能生效的设置，界面应明确提示并询问是否立即重启；用户选择稍后时应安全保存并继续，选择立即重启时应走受控重启流程。未来验收需覆盖确认、取消、重复提示、保存失败和重启失败恢复，且不得损坏存档、配置或游戏数据。

12. **启动检查更新与多平台资产选择**

   当前 Windows 启动检测到较新的匹配版本后，应从 GitHub 自动下载 binary 并更新；已有最新版时不应每次重复下载。GitHub 版本比较和 asset 选择机制属于未来设计；未来 macOS 与 Linux 使用对应平台资产（包括 AppImage）后再扩展。下载应先暂存并校验，退出后通过原子替换或 helper 完成更新；下载、校验、替换失败时应恢复并保护存档、设置和游戏数据。离线或 GitHub 不可用不得阻止启动。若没有公开 Release/version 资产，程序不能凭空获取更新，也不因此创建新的发布要求。未来验收需覆盖已是最新、可用更新、错误校验、断网、权限不足、回滚和多资产选择。

## 2026-09-09：Issue #12 生产验证完成

Issue #12 的 GC/render-thread flush 修复已在 0.4.20 源码阶段完成相称的生产路径验证，因此按规则仅将当前源码从 `0.4.19` 增加一个 patch；QOL13 仍对应 `0.4.19`。验证复用冻结 source 0.4.18 身份的 EXE（SHA 前缀 `7ccfdea7…`），实际使用 `17e3ab7`、现行 `poll_wait`，未启用 probe、overlay、delay 或 stale-skip。生产路径中 user08 独立读到 Map 109，普通 A 在 265.562 秒／tick 7363 越过旧崩溃点，完成 Kaim 分支对白并在关键 A 后的约 148 秒观察窗口内未崩溃；恢复移动发生在该窗口内；证据见 `out/issue12-triage/production-validation/run15-result.json`。

这证明当前生产交花路径在该覆盖范围内通过，不代表自然退出覆盖，也没有直接 GC flush 耗时测量；run-14 的 0.77 秒属于人工延迟诊断成本，不能当作生产基准。报告者验收、全游戏覆盖、额外候选回归和发布仍待完成，Issue #12 保持 OPEN；DLC 三包有界实际读取已通过，奖励和地下城仍 pending。0.4.20 版本记录为本地未发布内容；文档记录和必要验证分别处理，文档同步本身不增加版本，也不要求 Release。

## 版本规则与范围

- 当前发布版本保持 `v0.4.2`；v0.5.0 是规划里程碑，不要求本轮或每项需求完成时直接 bump 到 `v0.5.0`。
- 每项 feature 只有在实现完成并通过相称的必要验证后，才按当前实际版本增加一个 patch（`+0.0.1`）；这不要求同时发布 Release。仅记录文档、讨论或未完成实现/必要验证的实验不增加版本；用户验收与必要验证分别记录。
- 文件初始建立时的“本轮只记录”描述属于历史语境。当前状态是：未发布源码 0.4.23 保留 QOL1–QOL13 的记录，其中 QOL10 的原版菜单素材实现与限定直接检查已完成，用户已接受整体布局／风格方向；0.4.23 的简体中文标签、图形保存 UX 和 guest 返回路径已有有界实现与验证；QOL12（updater）保留其 updater 验证边界，Issue #12 GC 的有界生产交花验证单独记录；QOL13 仍对应 0.4.19 且其 fixture、生产编译和窗口集成已完成，125/150/200 实际桌面及跨屏覆盖仍未验证；QOL1–QOL12 及 bounded Windows Vulkan 验证保持各自证据边界；QOL6 已完成原安装目录两次 D3D12 `prepare-only` 启动验证，用户验收仍未确认。实现、验证、用户美术验收和发布状态仍分别记录。本次同步不新增构建、测试、Release 或 tag；DPI/DLC/菜单素材源码和版本记录仍为本地未发布内容。
