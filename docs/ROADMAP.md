# Roadmap

[简体中文](ROADMAP.zh-CN.md) · [Current status](STATUS.md)

Updated for v0.4.2 publication on **2026-09-08**. `[x]` means the stated scope has evidence, not that the entire game is complete. Dated notes retain earlier experiment states; [STATUS.md](STATUS.md) is the current release and validation ledger.

Legend: `[ ]` planned / outstanding · `[~]` in progress · `[x]` validated within the stated scope.

Work-item status and priorities are managed in the [public maintainer Project](https://github.com/users/freefrank/projects/3). This roadmap is its public English repository mirror, synchronized on demand by [project_manager](agents/project-management.md); implementation, validation, player acceptance and release evidence remain distinct.

## Current progress and next steps — 2026-09-09

- **Version and delivery:** Source and release target remain `0.5.0`. The latest local candidate is the position-evidence build documented in [release preparation](RELEASE-v0.5.0.md), with executable SHA256 prefix `6feb2092`; no public publication is claimed here. The existing GitHub draft and earlier ZIP remain historical until the final asset, tag and anonymous download are verified.
- **v0.5.0 work:** Map16 performance, DPI presentation, independent shader logging, optional shader collection and sparse camera temporal collection have bounded validation. Client build `0.5.0-position-evidence-1` adds conservative per-VS HLSL position-dataflow summaries and independently records camera, viewport, depth and matrix-finiteness check results before `UnknownShader` returns; it does not authorize jitter. The Worker accepts both schema 1 and 2, but no new executable game run or player schema-2 data exists. The four c7 paths are accepted as repaired in the same D-drive slot 02 Ghost Town — City of Ruins scene: six presented frames spanning about 11.37 seconds showed no flicker. This is not continuous-video or broader-scene coverage; shader-family regression, fullscreen, Alt+Enter, object/skinned MV and whole-game performance remain open.
- **P1 — implemented; reporter feedback pending:** [Issue #12](https://github.com/freefrank/LostOdysseyRecomp/issues/12) has an established GC/render lifetime race and a source repair merged locally from `claude-issue12` into `0.5.0` at `9cefb0d`. Production validation used the frozen source-0.4.18 EXE without probe, overlay, artificial delay or skip-stale, retaining `poll_wait` and the `17e3ab7` GC repair. A native 10-flower save independently loaded; ordinary A at serial 16 proceeded from confirmation to the branch follow-up and free Kaim movement within a 148-second crash-free, responsive observation window. The seed and EXE remained unchanged. This does not measure precise GC duration or cover the whole game. GC code `17e3ab7` was previously committed and backed up; the source 0.4.20 version increment and validation record are committed at `51b0cf0`. Target remains v0.5.0. The Issue stays open because the reporter has not received a repair binary or confirmed it. See the [root-cause report](notes/issue12-root-cause.md) and [handoff](notes/issue12-handoff-2026-09-09.md).
- **Next — development:** Direct3D 11 feasibility and runtime are retained as future work outside v0.5.0, with no assigned next-release version or date. The user accepted the CPU Vulkan comparison outcome; the earlier stopped acquisition has no retained paired metric, so no new benchmark figure is claimed.
- **Next — evidence or acceptance:** original-style settings and the reported font/return/save corrections have bounded validation; new player feedback is recorded separately; frozen-installer and DPI coverage remain open; two retained shader compiler errors are cached, not repaired. Player acceptance is separate.
- **Held or awaiting evidence:** Issue #9 is closed after project-user acceptance, without claiming original-reporter confirmation or a shared root cause. AMD-shadow investigations and enemy-disappearance flicker retain their existing pending or paused states. They are not resumed by this review. Issue #10 records future FSR work outside v0.5.0; Issue #11 is Done for user-confirmed Xenia → recomp direct-copy reading; reverse compatibility and conversion are outside this boundary.

## Published milestone: v0.4.2 — repairs and validation

The following fixes and bounded validation are complete. Original-reporter confirmation and broader progression remain separate work.

- [x] Repair the reproduced Council crash by using the guest's low 32 bits for word-switch dispatch. All 843 table contracts, 109 generated-code checks and the old-selector negative control pass. See the [Council root cause](notes/issue7-cutscene-crash.md).
- [x] Correct nine further PPC scalar, address and branch translation defects and synchronize the tracked dependency patch. Fresh generated code passes 3,258 native checks; the frozen old generator fails 1,533 matching cases. See the [implementation and excluded scope](notes/recompiler-width-audit.md).
- [x] Validate the exact `v0.4.1-issue7-semantics-r2` candidate through the full Council scene, restored Main Street movement, native save and independent restart/reload. The tested path uses Asia Disc 1, English, FXAA, 1280x720 and 30 FPS. Build/source audits and all 47 development-ZIP member hashes and CRCs pass; this does not establish a complete playthrough. See the [runtime and package evidence](notes/issue7-cutscene-crash.md#2026-09-07-follow-up-semantics-implementation).
- [ ] Obtain confirmation from the original Issue #7 reporters using their saves and hardware; the local reproduction does not establish equivalence to their unrecorded exception. The Issue is closed; this item tracks the remaining confirmation.
- [ ] Continue later-chapter, other-edition and hardware regression with native saves. Track naturally reached paths for the additional semantics fixes, including BLRL, without reopening the locally verified Council repair unless it recurs.
- [x] Six battle TAA paths pass 17,287 CPU checks and bounded Map3 32-phase scene/tire regressions; background ZIP and three-log retention pass independent fixtures and an actual export run. These results belong to their respective candidates; see [validation limits](STATUS.md).
- [x] Publish v0.4.2 from `2ed7a2d`: CI 34192600181, manifest/CRC, embedded version, hashes and anonymous download verification pass. Official-package checks cover build and artifacts; existing functional evidence is reused without repeated startup or game tests. See [release verification](STATUS.md).
- [ ] Continue the enemy-disappearance flicker investigation; the six-path repair does not resolve that report.

## Published milestone: v0.4.1

1. [x] Correct verified TAA lighting/depth jitter and shadow-reconstruction coordinate mismatches. Local implementation, integrated build and selected CPU/GPU checks passed; see [validation scope](notes/shadow-texture-lod.md).
2. [x] Add the two omitted scene-depth material passes after candidate `6d3bc037…` failed original-scene acceptance. r2 passes its integrated build, 8,192 CPU jitter checks and the same-position Map3 TAA/60 FPS/Auto 1440p draw checks. Near-tire ROI mean-brightness variation is lower; individual pixels still vary. See [comparison and limits](notes/shadow-texture-lod.md).
3. [x] Complete r2's original-Map3 TAA/AA Off comparison and player visual acceptance. The user confirms no tire flicker with TAA enabled, and the running EXE matches r2. The reported Map3 defect is resolved; other maps, motion and hardware remain regression coverage. The first candidate's failure is retained as history.

[v0.4.1](https://github.com/freefrank/LostOdysseyRecomp/releases/tag/v0.4.1) was published at 2026-09-08 00:03:50 UTC from `eb43f10`, including the accepted TAA repair and three-frame log-inclusive exports. Release CI 34171122945, all 45 manifest entries, installer self-test, eight headless startup-path checks and anonymous download verification passed. Official-package checks did not repeat GPU or player visual acceptance; the r2 evidence above retains that scope. The independent AMD first-battle and separate RX 9060 XT reports remain individually suspended. See [release evidence](STATUS.md).

<a id="v050-pc-graphics"></a>

## Next major milestone: v0.5.0 — PC Vulkan

Planned on **2026-09-07**, the Windows Vulkan feature is complete within a bounded local RTX 5080 scope: the `0.4.15` source tree is the historical Vulkan/package validation checkpoint (unreleased development build; public baseline remains v0.4.2), while the former source tree was `0.5.0`; the 22,933-shader corpus plus two retained DXIL-equivalent failures, Map3 Off, Map2 SMAA/TAA with D3D12 comparisons, resize/restart, Map12 TAA/history and cache recovery pass. Map12 recorded 253 ready/completed/reused frames out of 256; three capture stalls reset and recovered with zero jitter misses. Three raw frames were saved, and the production host archive verified all 273 entries; that host completion does not establish a natural in-game-worker exit. DX11 remains future work outside v0.5.0, retaining the working D3D12 baseline. Cross-GPU and full-game regression remain separate work. Linux/Steam Deck and Switch require separate platform work and acceptance. See the [Vulkan report](../out/v0.5.0/vulkan/REPORT.md).

Source `0.4.19` completes backend selection and typed cache isolation for D3D12/Vulkan. Selection policy defines capability minima, bounded fallback, cleanup, and explicit DX11 Unsupported behavior; cache envelopes bind backend, format, compiler, translator, options, and variant, and reject stale or malformed success files. Focused policy, lifecycle, cold/warm/restart, and framing checks pass, and the unique runtime build exited 0. Reserved DXBC recognition and DX11 failure behavior are not DX11 runtime implementation or validation. The D3D12/Vulkan scene-validation boundary remains Done / Validated from the user visual acceptance on 2026-09-08; DX11 runtime, fixtures, scene comparisons, and F1 captures remain future work outside v0.5.0. Broad cross-GPU coverage awaits user feedback and is not a v0.5.0 pre-release requirement.

1. [x] Implement typed D3D12/Vulkan selection and capability policy with explicit DX11 Unsupported handling, bounded fallback, and cleanup of partial startup state. Focused policy checks cover 21 staged failure/exception cases; the RTX 5080 native-device lifecycle probe created and released required D3D12/Vulkan objects without a window, guest, submission, draw, or present. DX11 runtime support remains separate.
2. [x] Vulkan batch 1: platform-independent shader, thread-lifecycle, memory-interface and copy/swapchain fixes are complete within the stated Windows Vulkan scope. Current guest timebase, D3D12 depth-clear fixes and submodule-plus-patch workflow remain preserved; Switch-only changes stay on its branch.
3. [x] Vulkan batch 2: DXIL/SPIR-V compilation and shader creation cover guest microcode and current built-in passes. The actual Vulkan corpus recorded 22,933 successes; the two remaining failures also fail the retained DXIL control and are not counted as Vulkan regressions.
4. [x] Bind D3D12/Vulkan success caches to typed backend/format/compiler/translator/options/variant identities and digest-checked envelopes. Focused checks cover identity, corruption, legacy rejection, cold/warm/restart reuse, and framing; isolated cold compilation made two actual DXC calls while warm/restart made zero. Reserved DXBC framing recognition is not DX11 compilation or runtime support.
5. [x] Vulkan batch 3: Windows SDL/WSI, runtime selection, DXC and loader/volk dependencies, descriptor/constant layouts, barriers, synchronization, resolve, readback and capture are complete within the stated Windows Vulkan scope.
6. [x] Vulkan presentation, SMAA, TAA/history, internal resolution and independent UI paths are complete within the stated scope, including resize and reset. Map12 has bounded history/capture-stall recovery evidence; broader scene and hardware regression remain separate.
7. [ ] **Future, outside v0.5.0:** Establish DX11 feasibility and minimum capabilities against the render abstraction: evaluate DXBC/Shader Model 5 compilation, per-draw resource/sampler remapping, slot/UAV limits, geometry shaders and synchronization/readback. Record unsupported features and fallback choices before committing to a design; the current backend and shader-format interfaces have no DX11/DXBC implementation.
8. [ ] **Future, outside v0.5.0:** Implement and connect the DX11 backend through compilation, cache, resource binding, rendering, presentation, SMAA/TAA, scaling/UI and capture. Track DX11 completion independently from Vulkan.
9. [x] The user visually accepted the bounded native-save D3D12/Vulkan Map2/Map3/Map12 real-scene and comparison boundary on 2026-09-08. DX11 validation, cross-GPU regression, and full-game coverage remain open; no DX11 support is implied.
10. [x] Validate the bounded native D3D12/Vulkan lifecycle: hidden 640×360→800×450 window/swapchain cycles, same-process re-init, failure cleanup, preserved external SDL reference, controlled restart, and SDL_QUIT close passed after the source `0.4.20` lifecycle repair. The fixture does not run a guest, draw, present, capture, save/profile/settings-file, or audio path; prior user scene acceptance is reused only for unchanged rendering behavior. DX11 runtime and broader hardware/lifecycle validation remain separate Todo work.
11. [ ] Collect actual-package rendering and stability evidence on available AMD, NVIDIA and Intel hardware, recording driver, edition, scene and settings. Mark unavailable hardware or scenarios pending, and keep player acceptance separate from automated checks.
12. [ ] **In progress:** Clean local source `0.5.0`, annotated tag `v0.5.0` and formal ZIP are complete at `86ba2c1641bb9a8324e0b1710783bead0c39bf23`. `LostOdysseyRecomp-windows-x64-v0.5.0.zip` is 44,080,866 bytes with SHA256 `100e6491548579e3a13aa60564bc760bca121113915842f73817d1b943482ce4` and source identity `896317f1a0ed86cea58fe353a2cb2104b4aa757efff4b72147e4da8ba2b82f7c`; 1 PCH, 4 TUs, 2 links and 1 package completed. Manifest/payload/hash/license checks passed; no functional tests, game runs or CI were run. `DELIVERY.json` and `REPORT.md` retain the full local evidence. No remote push, public Release or anonymous-download verification exists; public latest remains v0.4.2. Source-0.4.22/0.4.23 packages remain historical. DX11 and broad AMD/Intel coverage are future/user-feedback work.

### v0.5.0 quality-of-life backlog

Requested on 2026-09-08. These are separate Windows-first requirements alongside the PC-backend plan above. All twelve have completed this round's implementation and necessary bounded validation; player acceptance and release evidence remain separate for each stated scope. See the detailed [QOL requirements](notes/v0.5.0-qol-requirements.md). Each implemented feature with necessary validation increments the version by `0.0.1` without requiring a Release or mandatory player acceptance; v0.5.0 remains the target milestone and later cumulative increments do not force a minor-version change. The historical source `0.5.0` development candidate and its ZIP identity are recorded above, while the v0.4.15 ZIP remains historical evidence. The locally committed source 0.4.3–0.4.15 sequence does not revise those artifacts.

1. [x] Modernize the installer to recognize supported directory layouts, ISO images, extracted directories and XEX inputs; consider optional MD5/SHA-256 identification and improve the UI without weakening compatibility checks. Source and synthetic desktop flow validated; frozen-installer/DPI package coverage remains pending. Locally committed as `b094a1a98be441e24b8a14f2c02a2fd60020629a` (source 0.4.16).
2. [x] When `game-path.txt` is empty, resolve `default.xex` at the configured/root directory or `disc1` beneath an import root, with adjacent `game/disc1`, `game`, EXE and EXE-parent/game fallbacks. The actual main startup path reached the parser with the expected early exit.
3. [x] Refresh the executable icon from the original game icon while making the recompilation identity clear. ICO assets, resource and installer wiring, PE compilation and actual SDL icon-resource matching pass.
4. [x] Ensure the executable can start directly without BAT/CMD helpers, using its own directory as a stable resource base while preserving explicit-path precedence. The same real path forms (`default.xex`, `disc1`, adjacent `game/disc1`, `game`, EXE and EXE-parent/game) are supported; actual main startup checks passed before guest/GPU execution.
5. [x] Separate and modernize the first-launch configuration flow as its own UI/module without requiring an extra executable. The actual `IFileOpenDialog` picker handled >MAX_PATH and Unicode selections, cancel, caller COM preservation, and an incompatible-apartment error. Frozen-installer/DPI coverage, real retail input, player acceptance, and release remain separate.
6. [x] Repair repeated startup shader detection and compilation with a sequential startup bundle and identity-keyed deterministic failure cache. The original v0.4.16 installation had 22,972 valid compiled binaries plus two repeatedly rejected sources, rather than mass recompilation. After the local source `0.4.17` runtime update, two D3D12 prepare-only starts against its same original cache exited before guest startup: migration initialized in 10.781 s with two actual DXC rejections; the next warm start initialized in 3.634 s with a 77 ms metadata snapshot, 2,397 ms bundle load, 22,972 ready modules, two cached failures, zero source-content reads/translations/process-wide actual DXC calls, and 288 ready PSOs in 18 ms. The first run added only the bundle and two failure records; the second changed no cache entries, and five protected user files/cache metadata stayed unchanged. The build and `LoShaderStartupCacheTest` pass. This is bounded D3D12 preparation-only evidence: it neither repairs the two compiler errors nor proves Vulkan, gameplay, other platforms, player acceptance, or a release. See the [startup recurrence report](../out/v0.5.0/shader-startup-recurrence/REPORT.md).
7. [x] Verify active startup shader preparation uses logical CPU threads minus one (minimum one), including recovery. The production queue fixture and actual Map12 16-logical/15-worker startup sample pass; broader scene acceptance remains separate.
8. [x] Modernize and simplify the Debug Menu while retaining its diagnostic role. The narrow fixture covers child-focus F1/Esc/Gamepad B hide and busy/failure recovery; the r5 Map2 run displayed the real Debug window with correct map and coordinates while guest menus continued. Broader diagnostic flows, player acceptance, and release remain separate. Locally committed as `294df076e09c7a8b41c6b530a58ed59404ae24e0` (source 0.4.18).
9. [x] Replace the game’s built-in settings with host settings. Closing the self-made menu directly calls native apply `82870E38`, then close `82889E50`, preserving parent-menu completion and bypassing the original continue dialog. The focused actual-`menu.cpp` fixture and a real isolated Uhra → System → Settings save/back/reopen path pass; this is bounded implementation validation, not full-game or player visual acceptance. Source is 0.4.23, local and uncommitted.
10. [x] Recreate the original game settings style while retaining modern first-launch interaction. The source-0.4.22 run06 evidence records 38/0 and 41/2 original-font/GDI draw calls; its frozen 0.4.18 runtime EXE belongs only to earlier GC/DLC validation. Current source-0.4.23 menu runtime uses SHA256 `489649ceafd8005ef1e130633e0cfeef716d9581ba425cc342ad8b6b3a99667d`. Original-font Simplified Chinese labels now replace the prior GDI fallback: 反走样, 画面速率 and 保存图形设置; Root reviewed two native Publish previews and the actual new image. The user accepted the earlier run-06 overall layout and style; the new labels have direct implementation validation and no new user feedback. A real 0.4.23 development package exists, but no public Release is claimed; this also does not claim perfect reproduction, all-language acceptance, or broader gameplay/hardware coverage.
11. [x] When a saved setting needs restart, prompt for restart now or later and never force an immediate restart. Graphics now saves and applies in one click without Keep changes or a 15-second second confirmation; restart-required settings offer only Now or Later, and Back is Later. Storage/display failure restores the prior state. The focused fixture and actual isolated settings flow pass; normal child shutdown, player visual acceptance and full-game coverage remain separate. Source is 0.4.23, local and uncommitted.
12. [x] When a newer matching GitHub binary is available, automatically download and update it: Windows begins first, preserving save/settings/game data, verifying packages, replacing only after exit with rollback on failure, allowing offline startup, and reserving platform selection for future macOS/Linux AppImage packages. Core/helper/progress/main-entry checks and the v0.4.15 development ZIP identity gate pass: 47 manifest files, runtime/helper, three EXE icon payloads, DXC 2407 pair, licenses, provenance, README, and the no-game/source/PDB/user-data allowlist match. No public Release or player acceptance is claimed, and the package check did not launch a payload or repeat transaction/GPU validation. Locally committed as `9c2dc761a4c775b3ffc8b01429f11bd42f0c4f94` (source 0.4.17).
13. [x] Size the game main window and presentation area from the selected actual pixel resolution, without an additional Windows DPI multiplier. The video/window thread (PMv2) implementation keeps pixel sizing without extra scaling. Its direct hidden fixture passed two lifecycles at 96 DPI / 100% for 640x360 and 1280x720, with physical client, SDL drawable and cross-thread `GetClientRect` dimensions agreeing. Production validation compiled video/filesystem/GC (three translation units) and linked once; its real Map109 hidden-window check reported `PMv2=true`, 96 DPI and a 1280x720 client matching configuration, while the foreground remained unchanged. The 120/144/192 DPI cases cover the message contract only, not actual desktop or cross-monitor behavior. User visual acceptance and physical 125%/150%/200% or cross-monitor coverage remain untested. This feature version is 0.4.19; source was 0.4.21 when this validation completed, and current local source is 0.4.22, uncommitted; target remains v0.5.0.

## Published milestone and remaining coverage: v0.4.0

[v0.4.0](https://github.com/freefrank/LostOdysseyRecomp/releases/tag/v0.4.0) published at 2026-09-07 21:56:34 UTC from `40362d78`; release CI and official-package checks passed. Both audited editions passed isolated official-package Map2 startup at Auto 1080p/TAA with verified bundled compiler libraries. This is a bounded static scene check, not full-game or new player visual acceptance. Post-v0.3.0 test tooling is included. See [current release evidence](STATUS.md).

The earlier v0.4.0 checkpoint completed the recorded implementation and bounded validation for: text/UI, Debug, spatial AA/scaling, experimental TAA, sampled 60-FPS behavior and DLSS/FSR feasibility research. Wider gameplay remains regression coverage; no new user acceptance is recorded. See the [implementation checkpoint and acceptance criteria](notes/v0.4.0-development.md). The implementation is included in v0.4.0; the validation and acceptance limits below remain.

1. [x] Improve text clarity through output-resolution host Settings and supported pre-UI scene AA. Same-real-frame Navigation replays, production GPU controls and v7 actual final-AA skips verify removal of repeated AA from later UI; actual seven-row Graphics layout also passed. That v7 build kept guest UI at 720p; unsupported/CPU fallback and broader glyph/language quality are separate regression coverage.
2. [x] Add an independent English / Simplified Chinese Debug menu: fixtures, startup and actual Map2 tutorial-state EN → SC → EN checks passed, including dynamic map/no-battle/waiting-for-control text and restored language. Capture render state remains first. Other battle/status/layout variants are regression coverage; no new user acceptance is claimed.
3. [x] Add SMAA 1x: the upstream HIGH three-pass/LUT path passes selected GPU and actual pre-UI processing checks. Actual Graphics selection, Keep and same-process reopening passed, including switching SMAA to TAA. Further reference/scene comparisons are regression work.
4. [x] Add experimental camera-based TAA: normal AA3 settings, scene depth, jitter, stable-grid history/reset and final-AA bypass passed bounded v7 static/moving validation. Actual menu selection/Keep/reopening also passed. Unsupported paths use SMAA; native object motion is future work and broader scene quality remains regression coverage.
5. [x] Match actual output automatically with Standard/bilinear and High/bicubic quality: selected GPU/configuration and real-frame replay checks passed. Actual seven-row Graphics verified preview without disk changes, 15-second High rollback, Standard Keep and same-process close/reopen. This v7 spatial-filter change kept guest size fixed. True internal resolution is now an explicit follow-up below; process-restart/display coverage remains regression work.
6. [x] Implement 60 FPS with bounded correct-speed validation: sampled movement/dialogue, normal Apply/Keep/reopening and the 59.79827-FPS static camp passed. Natural 30/60 battle samples also show matching Ring timer/progress and long-hold timeout sequences; the observed 1.399/1.415-second phase boundary difference is within sampling granularity. Precise release/Perfect, damage and broader gameplay remain regression coverage; this is not whole-game locked 60. The optional unvalidated 120 candidate may be deferred.
7. [x] Complete DLSS/FSR feasibility research: official contracts, actual scene/depth/camera/jitter evidence, API/hardware/distribution risks and the staged implementation route are documented. Native object/skeletal motion, measured exposure/color-space semantics, broader scene coverage and vendor backends remain future engineering.

The user reports a closed-source implementation with correct speed at higher frame rates; its name and exact frame-rate coverage are unknown. It is a feasibility lead. Another implementation's limitation above 60 FPS does not rule out 120 FPS, but the latest user feedback permits a 60-FPS-only delivery if 120 FPS is difficult. Existing shadow investigations and unrelated backlog retain their prior status outside this new scope.

### Follow-up requested on 2026-09-07

The user reported no visible upscale improvement in any scene, following the earlier TAA feedback, and explicitly requested internal resolution up to 4K, preferably following output. The earlier execution checks remain valid for their builds; perceptible improvement in the new build is awaiting user review. The recorded development builds use `c548b48` as their base and include the follow-up changes; the subsequent implementation is included in v0.4.0. See the [follow-up handoff](notes/handoff-v0.4.0-followup.md).

1. [x] Automatically match combined Asian/USA-Europe bare-FPD and CPX metadata to imported resource identities, without a region option. Both editions index 52 resource files with zero CPX fallback and preserve all 20,686 source names/hashes against retained strict full scans; Disc 2 entry reuses the sibling-disc manifest. Existing profiles, unknown/failed extraction fallback, unread-content boundaries and explicit full-scan mode remain preserved. Fixtures and main build passed. Resource reads are 142,093,208/142,289,816 bytes; the observed 12.537/12.631 s are not a controlled speedup, startup or FPS result. The new package passed both-edition Map2 Auto 1080p checks with verified bundled libraries and source hashes; user data was preserved. Original battle/full-game acceptance remains outside this scope; previous single-edition timings remain [historical evidence](notes/shader-preparation.md).
2. [x] Add real internal-resolution Auto/output matching up to 3840×2160 plus manual 720p/1080p/1440p/2160p, independent of output and spatial-filter quality. The eight-row five-language settings implementation passed 212 CPU/menu checks and ten GDI renders; 37 dimension checks and 16 translated-shader DXC programs passed. GPU sampling and invalid-allocation recovery checks passed. At fixed 1080p output/AA3, Map2 still/moving runs verified native 720p, Auto 1080p and 4K source/depth/TAA inputs and observed history reuse without CPU/allocation fallback; inspected thin geometry and ground detail improved. Build-v3 Auto 4K output, legible Chinese eight-row Graphics, preview without disk changes, 15.017-second rollback, 1440p Keep and actual 1440p scene return/same-process reopening passed. The earlier v3 Auto 4K window reused history in 108/256 logged records; the identified camera-reference error is addressed below. New-build user visual acceptance and broader scenes/hardware remain unverified.
3. [x] Repair Issue #5's missing indirect-call entry at `0x82AFA388` and the analogous `0x82AFD150`. Four-disc boundary audit, regeneration, integrated build and 56 actual generated-dispatch/branch/script-cursor checks passed. The [reporter later supplied the USA/Europe save and confirmed the same encounter passed](https://github.com/freefrank/LostOdysseyRecomp/issues/5#issuecomment-5576195692); the Issue is closed. Other encounters and editions remain regression coverage.
4. [x] Add diagnostic coverage for Issue #6 startup allocation failures: original OS error, failing operation, mapping and memory context. 186 injected checks and real alias allocation/release passed without changing the mapping contract. The [maintainer closed the Issue with a v0.4.2 repair statement](https://github.com/freefrank/LostOdysseyRecomp/issues/6#issuecomment-5580296598); original-machine recovery remains unconfirmed, and the supplied log does not establish shared causation with #5.

5. [x] Correct the diagnosed TAA camera-history false rejection: the fixed .001 probe can fall at the projection infinity boundary, even with unchanged VP. Positive-W interval selection preserves pixel reprojection, validity thresholds, the quarter-screen limit and other history guards. Default CPU checks total 141; retained 512-pair replay passes 3,215 checks and reproduces all 158 original refusals. Integrated build and temporal GPU fixture passed. Native-720p and Auto 4K Map2 runs each recorded 256/256 history reuses, including 140/157 after input release, with no rejection gates or jitter misses and matching source/TAA/depth sizes. The same 24 active engine-tick input produced different final positions; this is not an identical-trajectory or FPS comparison. Broad TAA motion quality and user visual acceptance remain separate.

## Published milestone: v0.3.0

- [x] Implement and locally validate CPX/FPI discovery, verified XEX sources, bounded VS variants and previously recorded pipeline preparation; see [shader evidence](notes/shader-preparation.md).
- [x] [v0.3.0](https://github.com/freefrank/LostOdysseyRecomp/releases/tag/v0.3.0) published at 2026-09-07 06:55:19 UTC from unchanged tag `fba7ae4`; release CI 34091301175, package/installer checks and a short official first-battle smoke passed. No new player visual/stutter acceptance is implied; post-tag test tooling remains Unreleased.

## Previous milestone: v0.2.2

[v0.2.2 is published](https://github.com/freefrank/LostOdysseyRecomp/releases/tag/v0.2.2), including the accepted AMD resolve fix and Unicode startup/save paths. CI, official-package checks, eight startup-path cases and a Chinese-working-directory Map 12 smoke run passed. The complete Issue #4 gameplay crash remains unreproduced; see [release notes](https://github.com/freefrank/LostOdysseyRecomp/releases/tag/v0.2.2).

## Previous milestone: v0.2.1

[v0.2.1 is published](https://github.com/freefrank/LostOdysseyRecomp/releases/tag/v0.2.1), adding next-frame F1 render capture with automatic ZIP and combined SDL controller/keyboard input with E/R triggers. Hosted CI, including LoHidTest, and official-package hash, installer and isolated rendering/capture checks passed. Physical controller and gameplay switching coverage remains outstanding. This is not an AMD rendering fix. See [capture](notes/render-state-capture.md) and [input](notes/controller-input.md).

## Previous milestone: v0.2

v0.2 is published: both audited editions, edition-specific text/voice choices, import before setup and automatic installed-disc selection are included. The sets correspond to [Redump 11817: USA, Europe, version 0.0.0.3](https://redump.info/disc/11817) and [Redump 39111: Europe, Asia, version 0.0.0.4](https://redump.info/disc/39111). Import-time XEX hashes remain strict; this is not a complete ISO-to-Redump hash verification. Both original disc managers passed controlled 1 → 2 → 3 → 4 → 1 sequences, not chapter-boundary story progression. See [v0.2 notes](RELEASE-v0.2.md) and [disc selection](notes/disc-selection.md).

v0.1 remains the historical first Windows release with the importer, first-launch setup, five-language interface, shader index/parallel preparation and dialogue, ground-shadow, poster and window-responsiveness repairs. See [v0.1 notes](RELEASE-v0.1.md).

**The reported voice issue is resolved and user-confirmed.** The original and repaired vehicle dialogue sound normal. XMA continuation-packet handling now preserves new frames without changing sample rate or volume: the tested dialogue recovered from 2,129 to 4,062 frames, and its timing slope against the original changed from 0.547 to 1.000. Another 34 multichannel buffers / 4,264 frames decoded without errors. Other scenes and loop subframe boundaries remain routine regression coverage, not an open status for the resolved dialogue report. See [audio evidence](notes/audio-output.md).

## Near-term priorities

The following is the retained compatibility and regression backlog. The milestones above distinguish published work from future development.

1. [ ] Kaim's opening-battle body-shadow investigation remains suspended after the requested v0.4.0 recheck and final one-minute limit on 2026-09-07. Official-package tests on Radeon 8060S did not confirm the RX 9060 XT report; no root cause or fix is established. Resume with reporter video/log/settings and normal/abnormal captures. Map 13 remains unresolved, and a relationship is unknown. [Evidence and limits](notes/kaim-body-shadow-v040.md).
2. [ ] Diagnose attack-animation stutter using frame timings, shader-compilation logs and cold/warm-cache comparisons. The user's reply attributed it to shader compilation, but sampled evidence has not established that cause.
3. [~] Investigate blurry or garbled text against original-console and Xenia references; work in progress. This is separate from the paused text-language complement patch.
4. [ ] Regress shadow flicker during the fire-breath phase. The same player reported that it appeared to disappear; this is one observation, not a comprehensive fix claim. Fire-hit checker effects remain a separate open issue.
5. [x] **v0.5.0 — automatic game/DLC import is implemented:** the locally committed source [`0.4.15`](https://github.com/freefrank/LostOdysseyRecomp/commit/f77d943261e14105d17c880ffa40c171b4191dcc) batch imports Lost Odyssey CON/LIVE/PIRS STFS packages into a shared DLC store, with transactional extraction, duplicate/conflict protection and runtime enumeration/open/read. It removes the manual DLC/Game Discs mode choice and automatically classifies selected files, folders and mixed sources for one review and import flow. The existing three-package import and duplicate-preservation evidence is reused. The directory-filter repair keeps `spa.bin` out of `*.fpi`; native fixed 30-call evidence and a frozen source-0.4.18 runtime session read complete header/index/payload data for `LODLC002`, `LODLC001` and `LODLC003`, with zero `spa.bin` matches or crashes and a normal main menu. Thirteen imported files and isolated save/config stayed unchanged; both games exited. Reward collection, dungeon play, user acceptance and publication remain untested. Source was 0.4.21 when this validation completed, and current local source is 0.4.22, uncommitted; target remains v0.5.0.
6. [ ] **P0 v0.5.0 pre-release:** Split shader diagnostics from runtime summaries for the shadow/TAA investigation. The existing coverage-audit design (three session groups, cumulative inventory, F1 snapshots and full-draw records) remains a reusable reference; it is not automatically a required same-batch implementation scope. The log split is queued for the next session and has not started; see the [logging and coverage TODO](notes/taa-coverage-audit.md#shader-log-coverage-todo).
7. [ ] Triage [Issue #9](https://github.com/freefrank/LostOdysseyRecomp/issues/9): high CPU temperatures and TAA texture flicker reported on v0.4.2 with Ryzen 9950X3D / RTX 5090. Both symptoms are unverified; do not assume a shared cause or merge them with the earlier enemy-disappearance report.
8. [x] Profile steady-state Windows CPU hotspots in the fixed v0.4.15 D3D12 Map2 setup (Ryzen 7 9800X3D / RTX 5080, 60 FPS, SMAA, 720p internal and configured 1080p output). Over 45.0001416 seconds, the process used 110.875 CPU seconds (15.3993% machine CPU; 2.46388 logical-core equivalents); 112,344 on-CPU samples had zero lost events and 20 missing stacks. The threads carrying GPU-query polling and zero-timeout guest shared-value polling account for 40.389% and 39.994% of process CPU time (80.3833% combined). Their actual paths account for 36.5618% and 33.0948% of process on-CPU samples (69.6566% combined); the path shares are samples, not CPU-time attribution. GPU WorkerMain used 16.164% of process CPU time, and `getenv_nolock` is 24.6787% of that thread's exclusive samples. This isolates single-scene D3D12 paths only: it neither diagnoses temperature nor establishes a backend regression or whole-game behavior. GPU-query waiting, zero-timeout polling, and per-draw environment-lookup changes were candidates identified during profiling; their subsequent implementation is recorded in the next item. See the [hotspot report](../out/v0.5.0/cpu-hotspots/report/HOTSPOTS.html) and [CPU comparison](../out/v0.5.0/cpu-comparison/report/REPORT.html).
9. [~] Implement one CPU-efficiency batch from that profile: bounded waiting applies only to the measured GPU-query and shared-value polling paths, while four disabled-capture environment values are cached outside the draw hot path. Source `0.4.16` builds successfully and the new `LoPollWaitTest` passes. The valid new D3D12 Map2 comparison, without rerunning v0.4.15, is 4.3033598% mean / 0.6885376 cores over 60.0003575 seconds versus the retained 15.6200169% / 2.4992027 cores; 3,599 PresentMon events cover the complete CPU window on one swapchain, and bounded post-sample input remained in Map2. This is a single whole-batch result, not an attribution per change or full-game claim. The first Vulkan CPU window has zero overlapping PresentMon events and is excluded; the replacement acquisition was stopped by the user's physical Escape before acquisition. Vulkan comparison, player acceptance, and release remain pending. See the [optimization report](../out/v0.5.0/cpu-optimization/REPORT.md).
10. [~] **P0 before v0.5.0 publication:** source/release remain 0.5.0. Shader identity, vertex/index preparation and precise pacing fixes passed focused checks. Fixed Map16 D3D12 4K/TAA3 RTSS improved 48.01→54.05→59.76 FPS; final internal mean/p95 16.72/17.16 ms. This bounded near-60 result retains external-condition limits; other scenes and player acceptance remain open. See [diagnosis and repair](notes/v0.5.0-performance-diagnosis-2026-09-09.md).
11. [ ] **P0 v0.5.0 pre-release:** Diagnose and repair the reported 4K TAA ground/shadow blacking. A limited same-executable, same-process Map16 stationary TAA/Off reproduction exists: TAA encoded ground ROI was near-black in 100/180 frames and Off in 0/180, with 167 WGC callbacks for each slice. The failing shader and root cause remain unknown; similar-error candidates need matching log/state evidence and independent validation.

Existing work remains queued: intermittent GPU query/wait failures and long-session stability; fire-hit checker/crate effects and the two failing resource shaders; chapter-boundary, save/reload, encounter and multilingual regression for both editions; fullscreen/exclusive, mouse and mixed-DPI acceptance. Map 13 body/environment-shadow flicker remains open; the accepted ground-shadow fix remains regression coverage. The user has paused the Reddit post until the new performance, TAA ground/shadow and shader-log tasks are addressed; the v0.5.0 GitHub Release remains a draft.

## v0.2.2 delivery — 2026-09-06 local

- [x] AMD resolve fix `43ce0e53` accepted by the user and merged as `ab0d038`; NVIDIA RTX 5080 targeted checks and manual acceptance passed.
- [x] Unicode startup/save-path fix `7dbb668` merged as `04dd7d0`; eight startup-path cases and eight storage runs passed. The complete Issue #4 gameplay crash remains unreproduced; path tests do not establish in-game acceptance.
- [x] `v0.2.2` was published on 2026-09-07 at 03:11:10 UTC; tag `f03efe370d444db1a8a9c1213c240da697f58504` is on both remotes and CI `34077788392` passed. Official-package CRC/44 manifest hashes, installer self-test, eight startup-path cases and a Chinese-working-directory Map 12 smoke run passed.

The requested AA, scaling and frame-rate work now belongs to v0.4.0. **Text-language complement patch research was paused by the user on 2026-09-06**: no finished patch, no runtime code change and no inclusion in v0.2. Voice changes are outside that research scope. See [paused research](notes/text-language-patch.md).

## Phase 0: Preparation

- [x] Establish the repository skeleton and submodules.
- [x] Extract all four discs with `tools/god_extract.py` into the ignored `LostOdysseyRecompLib/private/disc1..4` directories.
- [x] Identify both supported XEX sets: Europe, Asia version 4 and USA, Europe version 3; title updates remain outside the audited sets. Match executable details and hashes rather than the region label alone; see [XEX evidence](notes/xex.md).
- [x] Generate the initial 841 jump tables with XenonAnalyse; install Ghidra 12.1.3 and XEXLoaderWV and import `default.xex` headlessly.
- [x] Run Xenia Canary through the opening battle and capture matching-camera references. See [comparison](notes/xenia-render-comparison.md).

## Phase 1: Produce compilable output

- [x] Populate the TOML configuration: save/restore addresses, invalid instructions, 80 explicit function boundaries, and setjmp/longjmp.
- [x] Generate XenonRecomp output with zero errors and zero unimplemented instructions in run 15; patches added 30 instructions.
- [x] Compile all 251 generated C++ files with clang-cl 22 and `/O2`, with zero errors. MSVC is not a supported compiler target.
- [ ] Translate every shader with XenosRecomp and record unsupported instructions; two known resource shaders still fail preparation.

## Phase 2: Reach the main menu

- [x] Restore the animated title background by correcting the level-0 packed-mip offset for small textures. [Evidence](notes/title-packed-mips.md).
- [x] Implement kernel HLE for threads, synchronization, memory, filesystem and XAM user profiles. The title loop runs in tested builds; its 60-fps swap rate is not an unlocked gameplay claim.
- [x] Render through plume and D3D12; the Press START title screen works. [GPU notes](notes/gpu.md).
- [x] Implement audio through Xenia FFmpeg XMAFRAMES decoding and SDL 48 kHz stereo output. Loop-end handling, cursor ownership, file-read races, command-register handling and dialogue packet skips have scoped fixes. The shared-handle test improved from 932 errors in 8,000 reads to zero; 32 consecutive MMIO kick/clear operations passed. The user has accepted the voice repair; other voices, long tracks and loop subframes remain routine regression coverage. Opening WMV playback remains unresolved. [Audio](notes/audio-output.md).
- [x] Support SDL controllers with keyboard fallback.
- [ ] Prevent IME interference with game controls: disable SDL text input/IME during gameplay controls and enable it only for actual text entry, without changing the user's global input method. Verify Z/X/A/S and F1 under a Chinese IME, focus changes and keyboard/controller switching. The user confirmed switching to an English input method restores controls on another PC; v0.2.1 remains unfixed. See [input evidence](notes/controller-input.md).
- [x] Disable controller rumble by default during development; `LO_CONTROLLER_RUMBLE=1` enables it.
- [x] Reach the title, main menu and settings after login and storage checks.

## Phase 3: Work toward a complete playthrough

- [~] Later encounters: repair opening-resource leftovers that caused Kaim's T-pose and stalled attacks. Normal attacks, counters and natural victories passed in independent tests; enemy poses and flicker still need investigation. [Encounter animation](notes/encounter-animation.md).
- [x] Provide the Windows debug victory path through the game's victory stage and result initialization; user-tested battle skipping works.
- [ ] Diagnose fire-hit rendering glitches with original-console references; Xenia also shows issues and is not a sufficient visual target by itself.
- [ ] Add temporary protagonist attack/damage adjustments to the debug menu, reversible and excluded from saves. [Requirements](debug-menu-requirements.md).
- [~] Save system: asynchronous completion, thumbnail ABI, persistent enumeration, NT writes and CREATE_ALWAYS semantics repaired. Manual saves, overwrite and independent-process reload passed. Broader compatibility remains unverified. [Storage](notes/save-storage.md).
- [x] Automatically select imported discs and reload their original indexes; both editions passed storage routing and original-manager switching tests. [Evidence](notes/disc-selection.md).
- [ ] Validate real chapter-boundary story transitions and subsequent save/reload on both editions; controlled manager tests do not establish full progression.
- [ ] Validate cutscenes, battles, A Thousand Years of Dreams and the world map individually.
- [~] Follow the walkthrough using isolated saves: Hypocenter wreckage/Ram and Ring tutorial, natural victories, early Wasteland encounters, Gorge camp, vehicle sequence and city-gate control have scoped evidence. The user reached Map 13. This is not a complete playthrough. [Walkthrough](notes/walkthrough-testing.md) · [Ring](notes/battle-ring-resource.md).
- [x] Fix black character silhouettes in the opening battle through correct physical-address aliasing: A/C share memory; E has a one-page offset. Windows/Linux alias tests and scene checks passed. [Evidence](notes/physical-alias-rendering.md).
- [x] Fix opening-battle broken geometry, later outline offsets and missing texture gamma through 16-bit index alignment, resolve dimensions and texture decoding. D3D12 numeric tests and scene comparisons passed. [Evidence](notes/rendering-index-and-resolve.md).
- [x] Restore opening-battle highlights through stencil support, D3D12 reference-value handling and D24 clear corrections. [Evidence](notes/lighting-stencil-depth-clear.md).
- [x] Fix whiteout in the real-time sequence after the first battle by converting EDRAM before drawing; the scene and muzzle fire return and the heavy-tank battle starts. [Evidence](notes/post-battle-whiteout.md).
- [ ] Resolve remaining crashes and complete a playthrough. The earlier RPBattle__Scene memset-truncation crash is fixed, but this does not close other stability issues. [Recompilation](notes/recomp.md) · [Handoff](notes/handoff.md).
- [x] Add same-map character teleport, coordinate bookmarks, return and axis adjustments. Native teleport/return, resumed walking and rejection during game menus tested. [Evidence](notes/debug-teleport.md).
- [x] Enumerate loaded-map POIs for saves, exits, mechanisms and pickups. Hypocenter save-point/mechanism destinations, exit proximity and rejection of old IDs tested. [Evidence](notes/debug-teleport.md).

## Current feedback and regression work

- [x] Ground projections: corrected stale guest pixel-shader use in mode-5 stencil draws; camp movement and targeted GPU tests passed, and the user reports projections are basically fixed. More maps/encounters remain regression targets. [Shadows](notes/shadow-texture-lod.md).
- [x] Map 12 poster black patches: polygon-offset repair passed a 300-frame A/B and shallow-depth occlusion regression. Broader near-plane/map coverage remains. [Poster evidence](notes/map12-poster-depth.md).
- [ ] Map 13 body/environment-shadow flicker remains open after the later user report; accepted ground projections are a separate repair. A shared cause with the poster defect is not proven.
- [ ] Fire-hit black/red checker effects; use original-console references because Xenia also glitches.
- [x] Ring outer ring: visible and changing in tested encounters; releasing RT produced Good and 101 damage in a recorded test. The user confirmed normal controller behavior. This closes the reported path, not every battle scenario. [Ring evidence](notes/battle-ring-resource.md).
- [ ] Black crate-destruction effects in the second map.
- [x] Debug map ID/localized name: unknown-title state, Hypocenter/Gorge and the map 2-to-3 transition tested. [Map information](notes/debug-map-info.md).
- [~] Save-anywhere: backend, non-save-point slot 03 save/restart/reload, restored permissions, native save points and camp transition tested. The reorganized F1 toggle has isolated UI size/callback tests; broader desktop/gameplay acceptance remains. [Save-anywhere](notes/save-anywhere.md).
- [~] Camp/window hangs: big-endian critical-section ownership/recursion repaired with four-thread and selected-route tests; window-message-pump changes are included. Separate GPU query/wait pointer corruption remains unresolved. [Critical sections](notes/critical-section-endian.md) · [GPU waits](notes/third-map-hang.md).
- [x] Voice issue resolved and user-confirmed: output and vehicle-dialogue timing repairs are published. Camp → vehicle sequence → city-gate control passed without the former stable decoder errors. Other scenes remain routine audio regression coverage; long-session stability is tracked separately. [Audio](notes/audio-output.md).

## Phase 4: Modernization

Compatibility and stability remain validation requirements. SMAA/TAA, resolution-aware scaling and stable, correct-speed 60 FPS are v0.4.0 targets; 120 FPS is optional and may be deferred. DLSS/FSR input feasibility remains research. Other features below remain later work; no release date is committed.

Future work also includes distinct bug fixes and shader optimization; DLSS, FSR and XeSS upscaling after the existing temporal-input prerequisites; frame generation research; the existing DX11, Linux/Steam Deck and new native macOS platform work; and the paused experimental Switch collaboration with u/Adoky. None is implemented or included in v0.5.0.

- [x] Parallel preparation with logical CPU threads minus one, minimum one: 15 workers on the test PC reduced 2,000-shader preparation from 53.4 to 6.7 seconds; successful outputs are byte-identical.
- [x] Startup preparation for known shaders, persistent cache and progress UI. Warm-cache reuse and damaged-DXIL recovery passed for the earlier 184-microcode set. [Preparation](notes/shader-preparation.md).
- [x] Discover 2,000 microcodes across four discs; 1,998 compile successfully. Progress display, cache reuse and an isolated Map 12 run passed.
- [x] Use the built-in 52-file shader index for supported resources, with scanning fallback for unfamiliar layouts. Local discovery dropped from 36.2 to 1.1 seconds; discovery and compilation timings are separate measurements.
- [x] Ship expanded CPX/XEX discovery, bounded VS variants and previously recorded pipeline preparation in v0.3.0; real draw reuse is verified within the recorded scene coverage.
- [ ] Resolve two microcode compilation failures and remaining source/variant coverage; source-only preparation of every first-use PSO is not implemented. First-use stalls in unseen scenes remain possible.
- [x] Replace settings while retaining original game options; add five-language UI, language choice, FXAA and aspect-preserving output scaling. [Settings](notes/settings-menu.md).
- [ ] Complete desktop acceptance for fullscreen/exclusive modes, mouse and mixed DPI.
- [ ] **P0 v0.5.0 pre-release:** Diagnose a possible render-output-size omission across desktop DPI transitions. Two physical 4K displays at 150% DPI showed a 4K internal/window observation paired with a 2560x1440 presented screenshot. The 100% comparison was a Parsec virtual 1024x768 DISPLAY2, where most of the 4K window was off-screen and the same process presented 4K; WGC only cropped edges. This is an unproven lead, not a comparison between two physical 4K displays, and remains separate from the completed window-pixel-size scope and the unrelated TAA flicker.
- [ ] **P0 v0.5.0 pre-release:** Diagnose the user-observed intermittent fullscreen condition where the picture sometimes does not fill the screen. Future acceptance must distinguish abnormal underscan from intentional aspect-ratio black bars; its relation to DPI or presentation sizing is unproven.
- [ ] **P0 v0.5.0 pre-release:** Add Alt+Enter window/fullscreen toggle, restoring the prior window size and position and debouncing key repeat to prevent repeated mode switches.
- [x] SMAA 1x and experimental camera-based TAA are implemented and verified at the bounded actual-game and menu selection/Keep/reopen scope above. Broader TAA quality remains regression coverage.
- [x] Add automatic output-resolution matching and Standard/High quality for v0.4.0: selected checks and actual seven-row Graphics preview/rollback/Keep/same-process reopening passed. True Auto/manual internal resolution up to 4K was subsequently implemented and validated at the follow-up scope above.
- [ ] Support widescreen/FOV changes with correct UI layout as later work.
- [x] Complete DLSS/FSR temporal-input feasibility research for v0.4.0, including actual evidence, missing inputs and future integration gates. No vendor backend is implemented; the published v0.3.0 DLSS placeholder is removed in v0.4.0 Settings.
- [ ] Investigate frame generation (FG) as later work; its disabled published control is removed in v0.4.0 Settings.
- [x] Implement 60 FPS and complete the bounded movement/dialogue, UI/camp and Ring core-timing/timeout checks for v0.4.0. Precise Ring release and broader gameplay remain regression coverage; whole-game locked 60 and the optional 120 candidate are not claimed.
- [ ] Add HDR output and tone mapping.
- [ ] Add higher-resolution shadows.
- [ ] Investigate SSAO and expose a clean depth buffer for ReShade.
- [ ] Complete the [v0.5.0 PC Vulkan milestone](#v050-pc-graphics); DX11, Linux/Steam Deck support, and their acceptance remain future work.
- [ ] Establish a native macOS port as separate future platform work, with no version or date commitment.
- [x] Ship the game importer and first-run setup with the hosted Windows v0.1 release. [Installation](INSTALLING.md) · [Packaging](notes/release-packaging.md).

## Phase 5: Optional exploration

- [ ] Screen-space GI / SSR.
- [ ] Hardware ray-traced shadows and reflections.
- [ ] High-resolution texture replacement and a mod loader.

- [~] Optional TAA shader collection: client consent/upload implemented; lo.dotslash.pro Worker/D1 accepts schema 1 and schema 2 telemetry. The conservative schema-2 position evidence is validated with retained inputs and Worker checks, including retransmit dedup; no new executable game run or real player telemetry is recorded. Ten captured vertex-path omissions repaired. Player UI/visual acceptance pending.
