# 2026-09-05 修复成果与交接

## 对白倍速修复（2026-09-05 最新结果）

用户已确认原始对白和修复后的游戏录音听感正常。XMA 跨包帧结束时曾额外跳过续接包中的新帧，导致对白被截短；现已保留这些帧，不改变采样率或音量。同段装甲车对白从 2129 帧恢复到完整 4062 帧，游戏录音相对原始音轨的时间斜率从 0.547 恢复为 1.000。另有 34 个多声道缓冲、4264 帧解码零错误。

本地验证构建 `135DCA79` 已安装，存档和配置文件校验未变。验证范围为该过场与上述音频样本；其他对白、战斗声音和循环子帧边界仍需覆盖。详细证据见 [音频记录](notes/audio-output.md)。下方较早实验的未完成状态按各自日期理解。

本报告汇总代码、专项记录与后台证据。代码现已提交：运行时 `2a4f47d`、调试与遇敌 `42c543f`、图形 `c4fc9e4`。本次用户明确授权提交并双推，游戏验证边界保持不变。完整九项状态见[状态总表](STATUS.md)。构建成功、后台验证和用户确认分别记录，不互相替代。

## 已取得的成果

| 工作 | 已完成与证据 | 仍有边界 |
|---|---|---|
| 随机遇敌动画/攻击停滞 | 资源恢复、宽 printf 与 switch 修正；普通攻击、反击、自然胜利通过 | 敌人地图姿态与光影没有因此全部解决 |
| Ring 外环 | 两次遇敌实际显示与变化；RT释放得到 Good、101伤害 | 只覆盖已测场景，不需要再为此重复 Perfect 测试 |
| 手动存档/随时存档 | 用户确认手动保存成功；本地可关闭的随时存档通过非存档点保存、重启读回；原生存档点与营地权限切换通过 | 桌面复选框点击和布局未验，不保证任意剧情中途保存安全 |
| 营地卡死 | 修复客体大端临界区递归数/拥有者处理；4线程、100000次递归回归及选定游戏流程通过 | 另有 GPU query/wait 指针损坏与偶发退出，未修复 |
| 音频 | 文件句柄定位读取加锁：8000次并发读取由932次错误降至0；32次连续XMA命令集成验证通过；修正消费者游标覆盖与显式游标更新后残留skip | 全部对白未听辨，非零PCM不能证明完整声音 |
| 音频完整路线 | `518AF9B5…`：营地→装甲车CG→城门并实际左移恢复控制；原稳定解码错误在该路线未再出现 | 不等于间歇崩溃已修好，也不等于全部声音已验证 |
| 调试功能 | 判胜已获用户确认；同地图/POI传送、地图ID与名称已有局部验证 | 攻击力倍率未实现，判胜满足快速推进的部分需求 |

相关证据：[战斗与Ring](notes/battle-ring-resource.md)、[随时存档](notes/save-anywhere.md)、[音频](notes/audio-output.md)、[临界区](notes/critical-section-endian.md)。标题动态背景、模型破面、首战后CG白屏等此前成果见[路线图](ROADMAP.md)，不计作本轮新增完成项。

## 阴影：根因、代码和验证边界

实际遇敌捕获 `shadow-encounter-01/f7254` 显示左上阴影图块有模型绘制，最终却全部为清除值1；非清除像素11574个全部在右上。后续 `shadow-clear-trace-01/f6918` 确认局部矩形 `(239.5,-0.5)..(399.5,215.5)` 被错误传播成整个 `880×896` 深度目标清除，擦掉此前图块。

已新增 `gpu/depth_clear_layout.h`：按EDRAM的80×16 sample tile、源/目标pitch和MSAA布局生成局部清除矩形，替换跨pitch整图清除。空矩形列表跳过API，避免“0个矩形代表清全图”。

- `tools/tests/depth_clear_layout_test.cpp`：左右图块隔离、pitch换行、目标外空覆盖、9种源/目标MSAA组合与逐sample地址枚举对照通过。
- `out/depth-clear-layout-test.log`、`out/build-depth-clear-layout.log`：测试与构建通过。
- 最新EXE SHA256：`10F4D144B6E1BF99546D9FA6BA35556FD5C187A1A61DBD68D822B998470DD3E1`。
- **这份新构建尚未在游戏中运行，不能宣布阴影修复完成。** 单采样host对部分MSAA覆盖仍是像素级近似；颜色别名清除未在此修改。

Polygon offset和显式零LOD也已接入并通过各自测试，但不能代替本次图块清除修正的游戏回归。火焰黑红格子、箱子破坏黑色效果仍开放。详见[阴影调查](notes/shadow-texture-lod.md)、[偏移](notes/polygon-offset.md)、[火焰](notes/fire-hit-rendering.md)。

## 下一步验收顺序

1. 使用最新EXE与独立Hypocenter存档/profile，后台进入相同遇敌；确认实际MSAA和清除区域，捕获左右阴影图块均保留及人物/敌人连续画面。
2. 同构建覆盖营地、火焰受击和箱子破坏，检查局部清除是否引入残留；不以不同镜头的像素差宣称改善。
3. 继续追GPU query/wait指针损坏；保留原始错误分支，不通过强制成功或空指针跳过掩盖。
4. 对背景音/对白做内容完整性验证；补随时存档桌面UI验证仅在得到相应操作授权后进行。

当前执行约束：后台独立副本，不操作桌面、不覆盖原始save/profile、不终止用户游戏、不自行commit/push。每30分钟heartbeat仅用于中断恢复，不证明始终有agent工作；上次查询连续goal为blocked，不能把调度状态当作修复完成。

## 证据与续接

`out/`中的游戏资产、原始深度、PCM和日志为本地私有证据，不提交。专项笔记的PID是记录当时的进程，继续前必须核对路径与存活状态：`13472/shadow-clear-trace-01`、`5408/shadow-encounter-01`都是旧构建，不能用于验证10F4D144。优先阅读本报告和STATUS，再按需追溯专项时间线；不要重复已通过的营地音频路线只换日志。
