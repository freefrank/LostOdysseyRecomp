# Mod package example

```ini
id=example-hd-texture
priority=100
image:bin/xenon/sys/engineresources.xxx#8:DefaultTexture=images/DefaultTexture.png
```

See [Modding API](Modding-API.md) for the canonical key format and manager contract.
