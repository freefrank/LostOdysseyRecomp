# Project status

Reviewed **2026-09-07** against the verified v0.3.0 release and the new v0.4.0 development scope. This page distinguishes published results from planned work; dated investigation notes retain the history of individual experiments.

## Current development target: v0.4.0

The user defined this target on 2026-09-07 and selected automatic output-resolution matching with quality presets. Local builds and selected tests passed; isolated runs cover Debug controls, actual 1080p host Settings, sampled 30/60 speed and the settings-driven experimental TAA path during Map2 movement. That earlier scope reached its recorded implementation and bounded-validation checkpoint, including final 30/60 Ring core timing. The user then reported no visible upscale benefit in any scene, following the earlier report of no visible TAA difference, and requested true internal resolution up to 4K, CPX indexing and Issue #5/#6 follow-up. These additions now have the bounded follow-up validation below; the earlier checks do not establish acceptance of the latest visual report. No new user acceptance is recorded; the changes remain Unreleased development work.

| Work | Target and validation boundary |
|---|---|
| Text clarity | Host Settings rasterizes text at output resolution; multi-size fixtures and actual 1080p output passed. The normal GPU path now processes supported scenes before UI and uses proven frame coverage to skip repeated final AA. Same-real-frame Navigation replays isolate the lettering changes caused by whole-frame AA; production GPU controls and v7 actual skips establish removal of that operation. The tested v7 build kept guest UI at 720p, and unsupported/CPU presentation retained the prior fallback. This does not establish every font's sharpness or guest redraw at output resolution. The seven-row Graphics layout and preview/Keep/reopen path passed actual v7 checks; further fonts and layouts are regression coverage. |
| Bilingual Debug menu | Independent English / Simplified Chinese switching and preview-safe persistence are implemented. Production-config/translation fixtures and a live startup EN → SC → EN cycle passed, covering 39 controls and saved-language restoration. A separate new-build Win32 check confirms Capture render state is first, visible without scrolling and clear of later controls. The same-window EN → SC → EN cycle also passed in actual Map2 tutorial state on PID 14032: map ID, no-active-battle and waiting-for-map-control statuses translated, English/language 0 restored, and Capture remained first. This closes the requested bilingual implementation at the tested scope; other battle/status/layout branches remain regression coverage. Evidence: out/v0.4.0-debug/live-dynamic-validation.md. |
| SMAA and TAA | Settings now select Off/FXAA/SMAA/TAA, retaining legacy migration. SMAA 1x HIGH passes selected GPU and actual pre-UI scene checks. Experimental camera-based TAA runs through normal AA3 settings without forced experiment flags: v7 retained 64 trace groups, 55 history reuses/9 resets, finite depth and preserved alpha, plus 95 processed copies with Full final-AA skips during actual Map2 movement. Unsupported paths fall back to SMAA; a newly detected unsupported transition may contain one already-jittered frame. Actual SMAA-to-TAA preview/Keep and same-process Graphics reopening passed. Native object motion remains absent; broader shader/transparency coverage is further regression. |
| Resolution-aware upscaling | Automatic viewport matching and independent Standard/bilinear or High/bicubic spatial quality are implemented; High is the settings default. AA runs at input size before scaling, with area-weighted downsampling in both modes and staged large reductions. Selected GPU/configuration checks and a real-frame six-mode replay passed. Those v7 spatial filters kept the guest rendering size fixed. True internal resolution is now an explicit follow-up requirement, tracked separately below. Actual High-to-Standard preview kept the file unchanged, 15-second timeout restored High, and Keep saved Standard through same-process close/reopen. This completes the tested quality-control path; process-restart and broader display coverage remain regression work. |
| Internal rendering resolution | Local settings/renderer implementation supports Auto following the effective 16:9 output region up to 3840×2160 and independent manual 720p/1080p/1440p/2160p. The eight-row Graphics page passed 212 configuration/menu checks and ten GDI renders across five languages at 720p/4K; a Simplified Chinese 720p render was visually checked. Square EDRAM targets retain native dimensions by allocation policy, and oversized targets keep a native fallback. The legacy `LO_RESOLVE_READBACK` path explicitly forces native internal size. Dimension rules passed 37 CPU checks; 16 translated sampling programs compiled with DXC. GPU numerical sampling passed 36 cases, alongside invalid-width rejection and subsequent valid allocation. Build-v2 isolated Map2 still/moving runs at fixed 1080p output/AA3 verified 1280×720, Auto 1920×1080 and manual 3840×2160 source/depth/TAA traces, observed history reuse and no CPU/allocation fallback. Thin geometry and ground detail improved with consistent UI alignment. Weather/particles differed, so this is not a pixel-MSE or performance comparison. Final build-v3 verified Auto 3840×2160 output and matching scene/depth/TAA input, a legible eight-row Chinese Graphics page, preview without disk changes, 15.017-second rollback, 1440p Keep, actual 2560×1440 scene inputs after leaving the menu and same-process reopening. The earlier v3 Auto 4K window reused history in 108 of 256 logged records; the diagnosed reference-point error and its correction are recorded below. New-build user visual acceptance and broader scenes/hardware remain pending. |
| TAA camera-history correction | The fixed .001 reference crossed the projection infinity boundary, producing false camera-cut rejection even for 114 bit-identical stationary VP pairs. Shared production/diagnostic positive-W interval selection repairs the reference points; pixel reprojection, thresholds, quarter-screen limit and other identity guards remain unchanged. The default CPU fixture passes 141 checks; 512 retained pairs pass 3,215 checks while reproducing all 158 original refusals with the old reference. Main/selected targets and the temporal GPU fixture passed. Native-720p and Auto 4K Map2 runs at 4K output/AA3/30 FPS each logged 256/256 ready/completed/history-reused frames, including 140/157 after input release. Each source/TAA/depth surface has 12 captures at 1280×720 or 3840×2160 respectively, with no rejection gates, jitter misses or allocation/CPU fallback. Equal 24 active engine-tick inputs ended at different positions, so this is not an identical-trajectory or FPS comparison. This does not establish full-game TAA quality or user acceptance. |
| Combined-edition shader discovery | Bare-FPD and CPX metadata automatically matches the audited Asian and USA/Europe resources using the existing identity logic, with no region option. Each edition matches 52/52 archive layouts and indexes 52 resource files with zero bare scanning or CPX fallback. All 20,686 source names/hashes equal each edition's retained strict full-scan baseline. Resource reads are 142,093,208 bytes for Asia and 142,289,816 for USA/Europe; USA/Europe previously read 20,770,329,949. Observed discovery was 12.537/12.631 s, but possible concurrent building prevents a controlled speedup claim. Starting from Disc 2 finds sibling discs and reuses the same manifest. Existing profiles are preserved; fixtures and main build passed. Required-microcode validation, unknown-layout/content fallback, unread-content limitations and explicit strict full scanning remain unchanged. The new package passed both-edition Map2 Auto 1080p checks with verified source hashes and bundled compiler libraries, without allocation fallback; original battle/full-game or new player acceptance is not claimed. See [current edition evidence](notes/europe-support.md#combined-resource-metadata--2026-09-07). |
| Issue #5 battle dispatch | Split the missing indirect-only entries at `0x82AFA388` and `0x82AFD150` from preceding function bounds. Instructions at the relevant boundaries match across all four local disc images; regeneration and the generated-library/integrated build passed. All 56 actual generated-dispatch, command-branch and script-cursor checks passed. The reported USA, Europe save was not supplied, so the original battle reproduction and user acceptance remain pending; no issue closure is claimed. |
| Issue #6 startup allocation | The five-line attachment reports guest address-space failure before game load, without operation or OS error detail. New diagnostics retain the original error before cleanup and report operation, view/address/size/offset, OS text and system memory context. 186 injected failure checks and real alias allocation/release passed without changing the mapping contract. Original-machine cause and recovery are unknown, and shared causation with #5 is unproven. |
| 60 FPS; optional 120 FPS | Persisted controls, host pacing and a narrow guest interval hook are implemented. Bounded Map2 movement and matched-dialogue timing support correct sampled 30/60 speed; the later short dialogue sample averaged 29.99293/59.89435 presents/s. These do not establish whole-game locked 60 or all animation/audio/cutscene behavior. Normal 30-to-60 menu application, Keep and same-process reopening passed without LO_FPS; runtime target was 60. The subsequent static 3D camp averaged 59.79827 FPS over 29 complete windows, with engine delta/wall 1.000034. The final natural-battle comparison passed Ring core timer/progress and the long-hold timeout path: phase4→5 first samples were 1.399/1.415 seconds, within 30/60 sampling granularity, and both returned to Attack. Precise release/Perfect, damage and whole-encounter duration were not compared. The 120 candidate requires `LO_EXPERIMENTAL_120=1`, otherwise falls back to effective 60, and may be deferred. |
| DLSS/FSR research | Feasibility research is complete: official versioned contracts, actual scene/depth/camera/jitter evidence, exposure/color-space, API/hardware/distribution risks and a staged implementation route are documented. Native object/skeletal motion, measured exposure/color-space semantics, broader input coverage and vendor backends remain future engineering. See out/v0.4.0-planning/research-delivery-audit.md. |

Evidence under `out/v0.4.0-dev/`: `build-integrated.log`, `presentation-result.txt`, menu-render outputs, `debug-live-switch.json`, `build-capture-top.log`, `capture-top-live.json` and `smoke-smaa-1080-seeded/{result.json,scene.png}`; configuration evidence is in `out/v0.4.0-debug-fixture/result.txt`. The language switch used build `84A041…`; capture placement and Settings output were independently checked on `8389d46…`. The first 90-second smoke remained in resource scanning; the seeded follow-up ran 58.0 seconds to 927 frames with no error/fatal entries and was cleaned up. The inspected output is host Settings, not guest glyph or SMAA game-scene acceptance. See the [implementation checkpoint](notes/v0.4.0-development.md#local-implementation-checkpoint--2026-09-07) for test scope and pending work.

The user reports a closed-source higher-FPS implementation with correct gameplay speed. Its name, exact frame-rate coverage and implementation are unknown; it is a feasibility lead, not local validation. A limitation reported for another implementation does not rule out 120 FPS. The latest user feedback permits a 60-FPS-only delivery if 120 FPS is difficult. Existing shadow investigations and other backlog items keep their prior status and are outside this new scope. See the [development scope and acceptance criteria](notes/v0.4.0-development.md).

The spatial-scaling build `AD2BF080…` introduced the seven-row Graphics page and removed unimplemented DLSS/FG placeholders while retaining five interface languages. Its build/GPU evidence is in `out/v0.4.0-dev/{build-upscaling.log,upscaling-result.txt}`. The earlier frozen `integrated-v7` baseline executable is `81752F19551DC111F6083CCCCAF383BA7F863ED5AB657592B7FB9F3DA3E682EF`, displaying `0.4.0-dev`. `out/v0.4.0-temporal/integrated-user-taa-v7/{validation.md,audit.json}` records normal AA3 execution and inspected static/moving outputs; `out/v0.4.0-text/clarity-delivery-v7.md` records the text-operation evidence. The owned run was cleaned up with its seed unchanged. The subsequent same-build Graphics run passed seven-row layout, quality preview/timeout rollback/Keep, and TAA/60-FPS application plus same-process reopening. Evidence: out/v0.4.0-fps/camp-graphics-six-v7/{README.md,camp60-summary.json}. Its stationary camp sample covers 1746 presents/29.198167 seconds (59.79827 FPS); original seed/exe were unchanged and the owned process was cleaned up. This closes those menu items. The final 30/60 Ring core-timing and timeout comparison is recorded in out/v0.4.0-fps/battle-v7-validation.md and the FPS addendum in out/v0.4.0-planning/completion-review.md; process-restart persistence, fullscreen, precise Ring release and broader scenes remain separate regression coverage.

The follow-up development builds were produced from base commit `c548b48` plus the changes recorded below; they are not published releases. Integrated main and selected test targets built successfully; `build-v1` EXE SHA256 is `F7A1BE32F483BF229F450B03B0B55B00F4FC72F5C1D6A865E90792A3F650DDF4`. Presentation and temporal GPU fixtures passed. A native-720p application launch recorded 34.132 s CPX discovery, 20,686 sources, 10,060 indexed packages/zero fallback and the same 9,781,918,469 resource-read bytes as the standalone candidate; subsequent first DXIL compilation was still in progress at that checkpoint. Current local evidence is under `out/v0.4.0-followup/`, including CPX source equivalence and timing, settings/GDI checks, four-disc boundary audit and memory-failure tests. See the [follow-up handoff](notes/handoff-v0.4.0-followup.md).

Previous follow-up artifact: build-v3 EXE SHA256 `44bfd437c630817b2bcdff82f06344b9723b7ffc6ff06cccb6468cd032b86058`. The `auto4k-final` result and menu evidence verify normal AA3 Auto 4K still/moving output, preview without INI changes, 15.017-second rollback, 1440p Keep and actual 1440p scene return plus same-process reopening. The harness terminated its owned process (exit 1); seed/EXE hashes stayed unchanged. `runtime-validation.json` also confirms all seven protected user files kept their hashes. Previous local ZIP `out/v0.4.0-followup/packages-final/LostOdysseyRecomp-windows-x64-c548b480-dev.zip` is 41,415,021 bytes with SHA256 `e3d5dd6ecec00d407677e871f80ec300377c38f8c936b838484b055eb20e3ee8`; all 45 manifest files and the dependency allowlist passed, the EXE matches v3, the packaged installation guide exactly matches the updated source, and packaged importer self-test exited 0. `out/v0.4.0-followup/package-final-audit.json` records this development package, including uncommitted follow-up work; it is not a published release or a clean `c548b48` build. No new user acceptance is recorded.

Earlier TAA-corrected development artifact: EXE SHA256 `017c2ba16f3bd56251a7770e7ace6f42617ad740345503ac4ea02ce5239c31d5`; ZIP `out/v0.4.0-followup/packages-taa-fixed/LostOdysseyRecomp-windows-x64-c548b480-dev.zip`, 41,418,024 bytes, SHA256 `f88ea47ced4a6556f184cdf432919bf467d9e0ab28d87819604dca108ab7f094`. All 45 manifest entries, the EXE and both compiler DLLs match the tested build; importer self-test exited 0 and no game/user data is packaged. Live Auto 4K process-module inspection confirmed the build-local compiler DLLs. Evidence: `out/v0.4.0-followup/package-taa-fixed-audit.json` and `taa-fixed-runtime-validation.json`; seven protected user files retained their hashes and both owned processes were ended by the harness (exit 1, not natural-exit validation). The preceding ZIP and investigation results remain unchanged; no commit, push, release or new user acceptance was recorded at that delivery checkpoint.

Earlier Asian-index CPX-direct development artifact: EXE SHA256 `3aae46b8d171cec4f977733a5a5f288d62d51aff7a1f0eeefbb098399509ebae`; ZIP `out/v0.4.0-followup/packages-cpx-direct/LostOdysseyRecomp-windows-x64-c548b480-dev.zip`, 41,787,161 bytes, SHA256 `79308a1508edc459360d8b0791329e719cd5bb75a100ab57a61fab5f23059cef`. Integrated build and the selected scanner fixtures passed. With empty discovery cache and reused task-owned DXIL cache, the actual build on the Asian edition recorded 13.335 s discovery, 142,093,208 resource-read bytes, 20,686 sources, 52 indexed files and zero scan/CPX fallback. Map2 Auto 1080p static output was inspected at effective 1920×1080 without allocation fallback. All 20,686 expected source hashes match inside the runtime cache; additional runtime sources are outside that comparison. All 45 package manifest entries and the tested EXE/DXC/DXIL match, importer self-test exited 0, and no game/user data is included. Seven protected user files retained their hashes; the harness ended its owned process (exit 1, not natural-exit validation). See the [current delivery evidence](../out/v0.4.0-followup/cpx-direct/DELIVERY.md). Earlier packages remain preserved; no new commit, push, release or wider gameplay/user acceptance is implied.

Current combined-edition artifact: ZIP `out/v0.4.0-followup/packages-both-editions/LostOdysseyRecomp-windows-x64-c548b480-dev.zip`, 42,612,002 bytes, SHA256 `ed8e0627f5398e2103f758009854ce29ee240f732565e66aeb2b19aa8b0295d5`; EXE SHA256 `c75946a3e83e7e1652f93646f332e51b19919b6a642cfd8ccda39a013cd3fe2e`. All 45 manifest entries, tested EXE/compiler DLLs and importer self-test passed; no game/user data is packaged. Both editions ran that packaged EXE with verified bundled DXC/DXIL and reached Map2 at effective 1920×1080/Auto/AA3 without allocation fallback; both static screenshots were inspected. Asian/USA-Europe discovery recorded 13.482/13.367 s, 52 indexed files, zero scanning and zero CPX fallback; these are observed discovery times, not controlled startup/FPS results. Each runtime's 20,686 expected source hashes equal the full-scan ground truth. Seven protected user files, task seeds and EXE stayed unchanged; the harness ended both owned processes. See the [combined-edition delivery](../out/v0.4.0-followup/cpx-both-editions/DELIVERY.md) and its `runtime-summary.json`. Earlier artifacts and scoped visual/issue limitations remain; v0.4.0 has not been released and no new player acceptance is recorded.

## Published release: v0.3.0

[v0.3.0](https://github.com/freefrank/LostOdysseyRecomp/releases/tag/v0.3.0) was published at **2026-09-07 06:55:19 UTC**, with draft/prerelease both false, from unchanged tag `fba7ae4` on both remotes. The successor workflow at `0a79e9e` explicitly checked out that tag; [release run 34091301175](https://github.com/freefrank/LostOdysseyRecomp/actions/runs/34091301175) passed. CPX/FPI discovery, XEX sources, bounded VS variants and recorded-pipeline preparation are included; suspended temporal-shadow diagnostics are excluded.

The 38,264,592-byte ZIP has SHA256 `db11b3f275bde905ce4796cee886c6d4787e9030ab55728541c134e3e660f485`; CRC/all 44 manifest entries passed and installer self-test exited 0. The official EXE (`9e9153e97a5caf1e107b1b9b36c9ed9c13a12d69dc6f6a6a0990f64aba201abe`) completed one isolated first-battle smoke run to frame 1,860. All 233 recorded recipes were ready in 21 ms with four workers, zero missing/failed; frame 1,800 showed 882,131 actual hits across 171 keys and zero runtime creates. A screenshot showed Kaim's battle menu; no error/fatal/critical logs were recorded. This short scene check is not shadow-flicker, FPS or new player acceptance. Evidence: original-workspace `out/release-v0.3.0/{published.json,package-validation.json,smoke-result.json}`.

Test organization changes after the tag remain Unreleased development tooling. Independent importer/shader/pipeline runs 34091299239 / 34091299255 / 34091299202 passed; they are distinct from packaging the tagged source. Existing startup/storage suites and the full A/B were not repeated for this package check.

Local validation generated 22,935 shader sources with 22,933 successful compilations and the same two failures; the new linked set covers two of 29 missing VS samples. The RTX 5080 pipeline A/B prepared 233 recipes in 22 ms and used 205 in 8,609,548 submitted draws; runtime creates were zero versus 220 with preparation off. Both runs had no logged >150 ms frames, so no stutter/FPS improvement is claimed. Driver/OS caches were not cleared, inputs were not frame-deterministic, and unseen scenes/first-use PSOs remain outside that proof. See [detailed evidence](notes/shader-preparation.md).

## Previous release: v0.2.2

[v0.2.2](https://github.com/freefrank/LostOdysseyRecomp/releases/tag/v0.2.2) is the previous full release (`draft=false`, `prerelease=false`), published on **2026-09-07 at 03:11:10 UTC** (2026-09-06 local), from `f03efe370d444db1a8a9c1213c240da697f58504`. The tag is on both remotes. [Hosted CI 34077788392](https://github.com/freefrank/LostOdysseyRecomp/actions/runs/34077788392) passed every step.

The official ZIP is 38,205,388 bytes, SHA256 `e91af2f49c03da48714731b07912767614d676be9d8887192647b217f2d29789`. CRC and all 44 manifest hashes passed; no private game data was present. The packaged installer self-test exited 0, and all eight startup-path cases passed against the official EXE. A Chinese-working-directory isolated RTX 5080 run remained alive for 49.54 seconds; swap 1200 visually showed Map 12. Two known shader-preparation failures remained with no new error/fatal records. The harness cleaned up its own process afterward; all 13 original save/profile hashes remained unchanged. Local evidence: `out/release-v0.2.2/{published.json,package-validation.json,smoke-result.json,ci.log,smoke-存档/scene.png}` in the original workspace. See [v0.2.2 notes](notes/release-0.2.2.md).

This release includes the accepted AMD resolve-initialization repair and Unicode Windows startup/save paths. The complete Issue #4 gameplay crash remains unreproduced; path/storage and smoke checks do not establish that crash is resolved.

## Previous release: v0.2.1

[v0.2.1](https://github.com/freefrank/LostOdysseyRecomp/releases/tag/v0.2.1) was the preceding full release (`draft=false`, `prerelease=false`), built from `906d7c039f7e709c57af2c5278a43e259bfba8e5`; both remotes have the tag. [Hosted CI 34053765472](https://github.com/freefrank/LostOdysseyRecomp/actions/runs/34053765472) passed every step, including `LoHidTest`.

The official ZIP is 38,203,314 bytes, SHA256 `cd583a6a28b47a1b2e7e31984052cd4eb6c953f5df333198f41540114eb4afb3`, matching the public API digest. All 44 manifest hashes passed and the packaged installer self-test exited 0 (`out/release-v0.2.1/package-validation.json`). An isolated cold-cache launch ran for 54 seconds with a visually confirmed German main menu. Its frame-400 capture ZIP contained 60 entries in 12,307,207 bytes; CRC and each entry SHA256 matched the raw files. ZIP completion at 43.798 seconds was followed by about 30 fps at 48.758–54.757 seconds. Evidence: `out/release-v0.2.1/smoke/runtime.log` and its captures. The test process exited, the main executable remained at `135DCA79`, and user saves were preserved.

## Previous release: v0.2

[v0.2](https://github.com/freefrank/LostOdysseyRecomp/releases/tag/v0.2) was published on **2026-09-06 at 17:16 UTC** as a full release, not a draft or prerelease. Its tag points to `dcc946299cdc2984783793ad5871a0ad0b90a2c9`; the code and tag were pushed to both remotes. [Hosted Windows CI](https://github.com/freefrank/LostOdysseyRecomp/actions/runs/34047138263) passed, including importer and disc-set tests.

The official `LostOdysseyRecomp-windows-x64-v0.2.zip` is 38,185,445 bytes, SHA256 `b125dae559a8d62d6ff79d0bcc80fd6af44fee6a41100db787b8bfca61facab6`. The downloaded ZIP matches the public API digest and checksum file. All manifest hashes, commit/version fields and `development_build=false` were verified; no original game files are included. The packaged importer self-test passed, followed by a 30-second isolated launch with a minimal PATH and a visually confirmed German title menu. Evidence: `out/eu-import-audit/release-v0.2-result.json` and `out/eu-import-audit/release-v0.2-smoke`.

The preceding [v0.1](https://github.com/freefrank/LostOdysseyRecomp/releases/tag/v0.1) was built from `2a3ffcc` and retains its historical release contents. Dated local/uncommitted notes describe their original test stage; USA/Europe and automatic-disc-selection changes are now included in v0.2. Source builds apply the tracked [dependency patches](../tools/patches/README.md).

**Validation remains limited to opening areas, selected scenes and controlled disc-manager requests. Chapter-boundary story transitions and a complete playthrough remain unverified.**

[Release notes](notes/release-0.2.2.md) · [Installation](INSTALLING.md) · [Build and packaging evidence](notes/release-packaging.md)

## v0.2 implementation and validation

The supported sets correspond to [Redump 11817, USA, Europe, version 0.0.0.3](https://redump.info/disc/11817) and [Redump 39111, Europe, Asia, version 0.0.0.4](https://redump.info/disc/39111). Their identity fields match the audited sets; a complete ISO-to-Redump hash comparison has not been performed. Both four-disc sets are accepted by the importer. Strict XEX hashes identify the edition; mixed editions are rejected both within an input set and when appending to an existing installation. Existing discs are checked from their actual XEX files. Runtime edition detection precedes setup/configuration, and missing-data first launch imports before presenting edition-specific game-language choices. USA/Europe offers English, Japanese, German, French, Spanish and Italian; the Asian game-language list and five interface translations remain unchanged.

The Release build, 19 importer tests and 11 isolated setup tests passed. All 60 files in a real four-disc import (23,063,969,792 bytes) matched the source ISO SHA256 hashes. All six language starts passed; English, German, French, Spanish and Italian title menus and the Japanese original brightness/settings page were visually confirmed. This covers menus/settings, not gameplay. The host voice selector now reads the resource-provided language list instead of assuming three choices; the five USA/Europe voice options and the three Asian voice options completed visually checked cycles. These changes are included in the **published v0.2 release**, not v0.1. The earlier language-support package passed all manifest hashes, frozen importer self-test and a 30-second rendered runtime check with PATH limited to System32. The main development executable and user save/profile were preserved. See [USA/Europe evidence and remaining coverage](notes/europe-support.md).

The published disc-selection change switches game paths to the requested installed disc before completing `XamSwapDisc`. It validates edition/disc identity, the original FPI and archive ranges, leaves existing handles open on their original files and preserves the current mount on failure. Both audited editions passed storage alias/read tests, and seven malformed/valid installation cases passed. The original disc manager completed 1 → 2 → 3 → 4 → 1 for both USA/Europe and Asia while reloading each target FPI and continuing rendering. The Asian final title menu was visually confirmed without a disc-change dialog. All 19 importer tests passed again, and the final Release build succeeded. The updated local package passed every manifest hash, frozen importer self-test and a 30-second rendered/captured launch with PATH limited to System32. Four storage write/read/overwrite/read-overwritten regressions also passed. This is controlled runtime coverage, not chapter-boundary gameplay or user acceptance. See [disc selection](notes/disc-selection.md).

## v0.2.1 capture

The capture and input changes below are included in the verified v0.2.1 release. Earlier local test results retain their original scope; final package validation is recorded above.

The F1 Debug Menu now queues **Capture render state** for the next complete frame, showing busy/progress/output status. It exports a resolved screenshot, draw register changes, translated shaders, intermediate resolve previews and raw resolve/depth data to `captures/render-<timestamp>-f<frame>` under the working directory, then automatically creates a sibling ZIP while retaining the raw files. The ZIP-enabled build and isolated archive validation passed: all 60 archived files matched their raw SHA256 values, with 91,363,708 bytes reduced to 11,701,650 bytes. The earlier build and two isolated RTX 5080 title/menu captures passed, including the actual F1 button command. Both exported matching BMP/PPM pixels, 16 correctly sized raw resolves and 21 shaders, then returned to about 30 fps. A deliberately invalid output directory reported failure without crashing and rendering continued; other disk failures, full layout/DPI and AMD behavior are not validated. This is included in published v0.2.1; it is not part of v0.2 and is not an AMD fix. Readback stalls invalidate performance comparisons, floating-point previews clamp values, and the output is not replayable. See [capture details](notes/render-state-capture.md).

## v0.2.1 input

All SDL-mapped controllers now merge into player 1 with the keyboard always available; no active-device selection is required. Hotplug uses stable instance IDs, buttons combine, triggers take their maximum and sticks choose the strongest vector above their deadzone. Keyboard events are captured on the event thread and cleared on focus loss; E/R add left/right triggers. Rumble remains opt-in and targets all opened pads. The actual HID implementation passed SDL virtual-device tests and the full build. The combined preview sustained an isolated title run near 30 fps, and a targeted Z event reached the SDL window loop; this is event-path coverage rather than physical keyboard/gameplay acceptance. Physical model/gameplay coverage remains unverified. This is included in published v0.2.1, and is not multiplayer support. See [input evidence](notes/controller-input.md).

## Implemented and validated features

| Area | Current result | Validation boundary |
|---|---|---|
| Importer | Introduced in v0.1; v0.2 accepts both audited four-disc editions from folders, XEX, XDVDFS ISO and GOD, with strict XEX hashes and mixed-edition rejection. | Complete imported-file hashes passed; import alone does not establish story progression. |
| First launch | Introduced in v0.1; v0.2 imports missing data before presenting edition-specific language/graphics setup, then initializes the game. | Eleven isolated setup tests and localized menu checks passed; the published-package smoke test also passed. |
| Settings | Original options/save confirmation, five interface translations, FXAA, output scaling and display modes retained. v0.2 chooses game text languages by edition and voice options from resources. | Both editions' voice selectors and six USA, Europe menu/settings languages tested. Fullscreen, mouse and mixed-DPI desktop coverage remains. |
| Shader discovery | v0.3.0 adds bounded CPX/FPI discovery, verified XEX sources and fixed/linked VS generation, while retaining naked-container indexes and fallback scans. | Local four-disc CPX/raw union: 20,686 byte-verified sources; with XEX and VS candidates: 22,935. First scanning/compilation can take minutes; caches are reused later. The historical 2,000-source index timing does not describe expanded scanning. |
| Shader preparation | Uses logical CPU threads minus one, minimum one, and persistent cache reuse. | On a 16-thread PC, 15 workers reduced preparation from 53.4 to 6.7 seconds in a controlled comparison. 1,998 successful DXIL outputs were byte-identical; two failures remain. Runtime variants and PSO creation can still cause first-use stalls. |
| Title and input | Animated title, menus, SDL controllers and keyboard input work in tested scenes. | Rumble is disabled by default; `LO_CONTROLLER_RUMBLE=1` enables it. |
| Saves and debug | Manual save/overwrite/reload, optional save-anywhere backend, map ID/name, coordinate bookmarks and same-map POI teleport implemented. F1 layout exposes the save toggle and supports resizing/scrolling. | Non-save-point save/reload, native save-point permissions and camp transitions tested. UI tests cover three sizes and checkbox callbacks; broader gameplay and multi-DPI acceptance remain. |

See [settings](notes/settings-menu.md), [shader preparation](notes/shader-preparation.md), [storage](notes/save-storage.md), [save-anywhere](notes/save-anywhere.md), [map information](notes/debug-map-info.md) and [teleport](notes/debug-teleport.md).

## Repairs and user feedback

**Fix verified and user accepted — 2026-09-06:** Full initialization of newly allocated placed resolve render targets fixes the tested AMD black title/background and depth-of-field path; framebuffer retirement now removes cached views safely. Technical review, AMD GPU regression/negative control, layout tests, title/opening-battle checks, and final-build copied-save/random-battle regression passed. NVIDIA RTX 5080 follow-up passed GPU/layout tests and title/settings/Map 12 comparisons without new darkening. The user then reported no glitches in the manual NVIDIA run; log review found no error/fatal or device-removed records and no new merge blocker, while retaining the documented shader warnings. Commit **`43ce0e53`** was merged and is included in published v0.2.2; it was not included in v0.2.1. NVIDIA battle and complete-playthrough regression remain additional coverage. See [resolve initialization evidence](notes/amd-resolve-initialization.md).

| Report | Current result | Remaining verification |
|---|---|---|
| Startup driver crash during depth clear | Batched rectangle clearing replaced the failing full clear path; targeted tests and startup runs passed. | Broader driver/hardware coverage. [Evidence](notes/startup-depth-clear-crash.md). |
| Ground character shadows disappearing after movement | Stale guest pixel shaders are no longer bound for mode-5 stencil-volume draws. Camp movement and GPU stencil tests passed; the user confirms ground projections are basically fixed. | Cross-map and encounter regression; not a claim about all character-surface shadows. [Evidence](notes/shadow-texture-lod.md). |
| Map 12 poster black patches | Polygon-offset conversion corrected. Same-binary 300-frame A/B: legacy path had patches in 169 frames; corrected path had none. Shallow-depth occlusion test passed. | Broader maps and near-plane behavior. This approximates float24 behavior rather than fully emulating it. [Evidence](notes/map12-poster-depth.md). |
| Map 13 body/environment-shadow flicker | Later user feedback still reports alternating light/dark shadows; this remains open. | Find a controlled bright/dark pair and the responsible draw/state. The accepted ground-projection repair is separate; a shared cause with the poster issue is unproven. |
| Ring outer ring missing | User confirmed normal behavior with a physical controller. Hold RT after confirming an attack, then release when the rings overlap. No additional rendering fix was required for the final report. | Other battle scenarios are not exhaustively tested. [Evidence](notes/battle-ring-resource.md). |
| Accelerated / unintelligible dialogue | **Resolved and user-confirmed.** XMA packet handling preserves new frames in continuation packets without changing sample rate or volume. The tested vehicle dialogue recovered from 2,129 to 4,062 frames; timing slope against the original changed from 0.547 to 1.000. User confirmed normal playback. | The reported voice defect is closed. Other scenes and loop subframe boundaries are routine regression coverage. 34 multichannel buffers / 4,264 frames also decoded without errors. [Evidence](notes/audio-output.md). |
| Camp deadlock and window unresponsiveness | Big-endian critical-section deadlock repair and window-message-pump changes are included. Selected routes and focused tests passed. | Separate intermittent GPU query/wait pointer corruption remains unresolved; successful routes do not establish complete stability. [Critical sections](notes/critical-section-endian.md) · [GPU waits](notes/third-map-hang.md). |

Older executable hashes in investigation notes identify test artifacts, not the current release executable. User save/profile preservation checks belong to those recorded runs.

## Open issues

- **Issue #5 battle freeze:** missing generated indirect-call entries have a local repair with 56 actual generated-function checks passed; the reported USA, Europe battle and user acceptance remain pending.
- **Issue #6 startup allocation:** failure diagnostics are implemented and tested, but the original attachment cannot establish an OS operation, error code or root cause. A successful local allocation does not establish recovery on the reporting machine.

- Fire-hit black/red checker effects and black broken-crate effects have no verified fix.
- Intermittent GPU query/wait failures and long-session stability still need diagnosis.
- Two resource-shader translations fail; unrecognized resources, runtime variants and PSO preparation remain incomplete.

## Regression coverage

Experimental TAA still needs broader scene and motion-quality coverage; the diagnosed invalid camera-reference rejection is a separate local repair described above.

The reported voice problem is resolved, as reconfirmed by the user on 2026-09-06. The following are coverage tasks, not evidence that the voice defect remains open.

- Map 13 body/environment-shadow flicker remains open after later user feedback; accepted ground projections remain routine regression coverage.
- Additional audio scenes, encounters, cutscenes, A Thousand Years of Dreams and world-map progression require regression testing.
- Chapter-boundary saves/reloads and multilingual text/voice playback across both editions need coverage beyond the menu tests.
- WMV playback, chapter-boundary progression and a complete playthrough are not validated; controlled installed-disc selection is recorded separately above.
- Fullscreen/exclusive modes, mouse interaction and mixed-DPI behavior need broader desktop acceptance testing.

## Next development goals

The current target is [v0.4.0](notes/v0.4.0-development.md), with the requested text/UI, AA, spatial scaling, 60 FPS and feasibility research implemented and validated at the recorded scope. The [roadmap](ROADMAP.md) retains broader compatibility and regression coverage; development remains Unreleased, with no committed release date.

**Published v0.3.0 retains disabled DLSS/FG placeholders; local v0.4.0 removes those controls.** Standard/High spatial filtering is independent of the implemented Auto/manual internal rendering resolution up to 4K; it does not provide vendor temporal upscaling. SMAA, experimental camera-based TAA and 60 FPS have completed the bounded checks above; further scenes, fonts and precise Ring release remain regression coverage. Broader internal-resolution compatibility remains regression work, and optional 120 FPS work may be deferred. [DLSS/FSR feasibility](notes/temporal-upscaling-feasibility.md) is complete; vendor backends, native object motion and measured color/exposure semantics remain future engineering. HDR, frame generation, Linux/Vulkan gameplay and Steam Deck validation remain later work.

## Paused research

The user paused the text-language complement patch on **2026-09-06**. There is no finished patch or runtime code change, and it is not included in v0.2. Voice is outside that research scope. Keep it out of active implementation priorities unless the user resumes it; see [research record](notes/text-language-patch.md).

## Maintenance

[Roadmap](ROADMAP.md) · [Handoff](notes/handoff.md) · [Research index](notes/README.md) · [Work report](WORK_REPORT_2026-09-05.md) · [Archive](archive/README.md)

Private game data and raw diagnostic captures are not distributed. Selected gameplay screenshots in `docs/images/` illustrate development builds. Record implementation, release inclusion, test scope and user feedback separately when updating this ledger.
