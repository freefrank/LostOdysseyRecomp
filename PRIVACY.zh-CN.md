# 隐私与诊断收集

《Lost Odyssey Recomp》的自动诊断收集是可选的。默认未同意上传，程序通过现有五语同意设置询问，并可在现有设置项中关闭。关闭自动收集后，排队等待自动上传的内存样本会被丢弃；已经发出的请求可能完成。程序不会生成跨会话的玩家或设备身份。

手动 F1 捕获会写入本地诊断包；源码扩展为该包增加程序产生的 VS/PS 原始微码，并另设自动增量上传路径。F1 捕获独立于自动收集同意，由用户手动触发。自动载荷可能包含 GPU 型号、驱动和后端标签、构建与分辨率信息、程序生成的 VS/PS shader 哈希、原始微码、shader 状态标志，以及在受支持路径产生时的稀疏相机／深度／运动样本、抖动和矩阵。

程序上传载荷和 D1 中持久化的结构化诊断记录不含账号用户名、邮箱、机器主机名、本地文件路径、序列号、硬盘序列号、MAC 地址、设备 UUID、安装 ID、Cookie 或 tracking ID。不上传存档、个人文件或用户写入的 shader 源码。源码扩展中的原始微码是程序产生的捕获内容，不是本项目源码，也不是用户个人文件。服务端按内容阶段和 SHA-256 内容身份保存一份，并把 GPU 型号作为诊断元数据关联；不会按用户或物理设备分库存储。D1 中 30 天未更新的记录会删除。

Worker 只读取 `CF-Connecting-IP` 并将其交给平台临时限流器；不会把该值写入 D1，也不用于跟踪用户。本收集 Worker 已关闭持久 Workers Logs、invocation logs 和 traces。Cloudflare 为了传输和安全处理，仍会处理来源 IP 及正常 HTTPS 请求元数据；本文不承诺 Cloudflare 或其他基础设施的所有日志都关闭。

手动 F1 捕获是用户触发、写入本地并供用户检查和分享的诊断包，可能包含 `runtime.log`，因此可能出现本地路径或其他诊断文字；分享前应先检查。自动收集是另一条需要同意的上传路径，使用有限记录、按内容去重并遵守上述保留期限。自动收集不会阻塞游戏：使用预分配的有界内存，try-lock 忙或存储已满时跳过，不在渲染热路径执行 I/O、分配或等待，并使用独立上传状态。停止或退出不会等待网络请求。这些边界不承诺 CPU 代价绝对为零。

F1 本地捕获是独立的用户操作，不会开启自动上传。手动捕获完成后，只有在自动收集已经同意时，程序才会在后台尝试上传其待处理的 VS/PS 和结构化批次；捕获过程不会改变同意设置。

schema 3 绑定记录同样受现有同意开关控制。它为 TAA 诊断增加有界的消费者、纹理和生产者状态证据，包括矩阵值以及帧龄／resolve gap 字段，但不包含指针、地址、路径、身份或图像数据。该数据仅用于 renderer 研发。

compact 自动诊断流同样受现有同意开关控制。其协议每 180 秒最多采集一个固定 32 帧窗口，最多包含 24 对 VS/PS 和 8 条绑定记录，并附带 TAA history 状态与 rejection 掩码、覆盖计数、待发送数量和投递结果。它可以包含现有的 stage/FNV shader 引用及结构化 schema 2/3 记录，但不包含色彩图像预览、F1 ZIP 原始内容，也不生成随机、会话、玩家或设备标识。compact 流是独立的 opt-in 上传路径；现有 F1 捕获仍是独立的手动操作。

D1 的 30 天未活跃记录过期是在线诊断服务的运营保留规则。私有 GitHub 研发归档仓库 `LostOdysseyRecomp-build-inputs` 每天接收按内容寻址去重的快照，长期用于研发，访问范围限于维护者和 CI。私有 Git 副本不沿用 D1 的自动过期；从最新目录删除文件也不会抹除此前的 Git 历史。

See the [English privacy document](PRIVACY.md).

当前字段和验证边界见 [TAA 收集说明](tools/taa-collector/README.md)。该收集用于诊断研究，不能据此宣称渲染修复或完整游戏验收。
