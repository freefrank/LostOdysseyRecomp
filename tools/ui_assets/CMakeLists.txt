cmake_minimum_required(VERSION 3.20)
project(LoUiAssetDecoder LANGUAGES CXX)
get_filename_component(ROOT "${CMAKE_CURRENT_SOURCE_DIR}/../.." ABSOLUTE)
add_executable(LoUiAssetDecoder decode.cpp "${ROOT}/thirdparty/lzokay/lzokay.cpp")
target_compile_features(LoUiAssetDecoder PRIVATE cxx_std_20)
target_include_directories(LoUiAssetDecoder PRIVATE "${ROOT}/LostOdysseyRecomp" "${ROOT}/thirdparty/lzokay")
if(MSVC)
    target_compile_options(LoUiAssetDecoder PRIVATE /utf-8)
endif()
