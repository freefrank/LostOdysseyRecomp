diff --git a/LostOdysseyRecomp/gpu/renderer.cpp b/LostOdysseyRecomp/gpu/renderer.cpp
index f924405..95a5283 100644
--- a/LostOdysseyRecomp/gpu/renderer.cpp
+++ b/LostOdysseyRecomp/gpu/renderer.cpp
@@ -6395,2 +6395,6 @@ void main(triangle V input[3], inout TriangleStream<V> stream)
 
+#if !defined(LO_RENDERER_P2_EMBEDDED_TEST)
+    // The asset-free fixture instantiates Renderer directly. Exclude only game
+    // entrypoints from that TU, not the production methods under test. COFF
+    // exception funclets may otherwise retain unused game startup call graphs.
     bool Init()
@@ -7113,2 +7117,3 @@ void main(triangle V input[3], inout TriangleStream<V> stream)
 #endif
+#endif // !LO_RENDERER_P2_EMBEDDED_TEST
 #else
@@ -7129,2 +7134,3 @@ void main(triangle V input[3], inout TriangleStream<V> stream)
 #endif
+#if !defined(LO_RENDERER_P2_EMBEDDED_TEST)
     void SetOutputSize(uint32_t width, uint32_t height)
@@ -7178,2 +7184,3 @@ void main(triangle V input[3], inout TriangleStream<V> stream)
     }
+#endif // !LO_RENDERER_P2_EMBEDDED_TEST
 }
diff --git a/tools/tests/native_dlss_renderer_gpu_test.cpp b/tools/tests/native_dlss_renderer_gpu_test.cpp
index 1e26801..e1f4ac8 100644
--- a/tools/tests/native_dlss_renderer_gpu_test.cpp
+++ b/tools/tests/native_dlss_renderer_gpu_test.cpp
@@ -2,2 +2,3 @@
 // are linked. The only substituted GPU operation is the proprietary NGX call.
+#define LO_RENDERER_P2_EMBEDDED_TEST 1
 #include <gpu/renderer.cpp>
