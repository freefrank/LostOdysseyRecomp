diff --git a/XenonAnalyse/function.cpp b/XenonAnalyse/function.cpp
index 34add408f6905c4ef6a3ab8588d1f62c36ea9a08..ea3672ef98973135cb3f76cc9d8fc8c4e6414e24 100644
--- a/XenonAnalyse/function.cpp
+++ b/XenonAnalyse/function.cpp
@@ -180,13 +180,17 @@ Function Function::Analyze(const void* code, size_t size, size_t base)
                 }
                 else if (op == PPC_OP_CTR)
                 {
-                    // 5th bit of BO tells cpu to ignore the counter, which is a blr/bctr otherwise it's conditional
-                    const bool conditional = !(PPC_BO(instruction) & 0x10);
+                    // BO[0] (0x10) makes the branch ignore the CR condition and BO[2] (0x04)
+                    // makes it ignore the counter; only with both set is this an
+                    // unconditional blr/bctr. bdzlr/bdnzlr (BO 0x12/0x10) still fall
+                    // through when the counter disagrees, e.g. the byte tail of the CRT
+                    // memset ("stb; bdzlr; stb; bdzlr; stb; blr").
+                    const bool conditional = (PPC_BO(instruction) & 0x14) != 0x14;
                     if (conditional)
                     {
                         // right block's just going to return
                         const size_t lBase = (addr - base) + 4;
-                        size_t lBlock = fn.SearchBlock(lBase);
+                        size_t lBlock = fn.SearchBlock(base + lBase);
                         if (lBlock == -1)
                         {
                             blocks.emplace_back(lBase, 0);
diff --git a/XenonRecomp/recompiler.cpp b/XenonRecomp/recompiler.cpp
index fa915279059d564632327ddf6ad67b3bcefa8835..86309dcf508039091b1ba5337fea2cabdafe096a 100644
--- a/XenonRecomp/recompiler.cpp
+++ b/XenonRecomp/recompiler.cpp
@@ -609,7 +609,10 @@ bool Recompiler::Recompile(
     case PPC_INST_BCTR:
         if (switchTable != config.switchTables.end())
         {
-            println("\tswitch ({}.u64) {{", r(switchTable->second.r));
+            // Switch-table indices use the guest word value, as do their
+            // bounds checks and address calculations. A preceding 64-bit addi
+            // may retain a carry (0xFFFFFFFF + 1) above that word.
+            println("\tswitch ({}.u32) {{", r(switchTable->second.r));

             for (size_t i = 0; i < switchTable->second.labels.size(); i++)
             {
@@ -635,7 +638,7 @@ bool Recompiler::Recompile(
         }
         else
         {
-            println("\tPPC_CALL_INDIRECT_FUNC({}.u32);", ctr());
+            println("\tPPC_CALL_INDIRECT_FUNC({}.u32 & ~3u);", ctr());
             println("\treturn;");
         }
         break;
@@ -643,7 +646,7 @@ bool Recompiler::Recompile(
     case PPC_INST_BCTRL:
         if (!config.skipLr)
             println("\tctx.lr = 0x{:X};", base + 4);
-        println("\tPPC_CALL_INDIRECT_FUNC({}.u32);", ctr());
+        println("\tPPC_CALL_INDIRECT_FUNC({}.u32 & ~3u);", ctr());
         csrState = CSRState::Unknown; // the call could change it
         break;

@@ -663,10 +666,12 @@ bool Recompiler::Recompile(
         break;

     case PPC_INST_BDNZF:
-        // NOTE: assuming eq here as a shortcut because all the instructions in the game do that
+    {
+        constexpr const char* fields[]{ "lt", "gt", "eq", "so" };
         println("\t--{}.u64;", ctr());
-        println("\tif ({}.u32 != 0 && !{}.eq) goto loc_{:X};", ctr(), cr(insn.operands[0] / 4), insn.operands[1]);
+        println("\tif ({}.u32 != 0 && !{}.{}) goto loc_{:X};", ctr(), cr(insn.operands[0] / 4), fields[insn.operands[0] % 4], insn.operands[1]);
         break;
+    }

     case PPC_INST_BEQ:
         printConditionalBranch(false, "eq");
@@ -712,7 +717,13 @@ bool Recompiler::Recompile(
         break;

     case PPC_INST_BLRL:
-        println("__builtin_debugtrap();");
+        // The branch uses the old LR; setting the return address must not
+        // replace the indirect call target.
+        println("\t{}.u32 = uint32_t(ctx.lr) & ~3u;", temp());
+        if (!config.skipLr)
+            println("\tctx.lr = 0x{:X};", base + 4);
+        println("\tPPC_CALL_INDIRECT_FUNC({}.u32);", temp());
+        csrState = CSRState::Unknown; // the call could change it
         break;

     case PPC_INST_BLT:
@@ -729,7 +740,7 @@ bool Recompiler::Recompile(

     case PPC_INST_BNECTR:
         println("\tif (!{}.eq) {{", cr(insn.operands[0]));
-        println("\t\tPPC_CALL_INDIRECT_FUNC({}.u32);", ctr());
+        println("\t\tPPC_CALL_INDIRECT_FUNC({}.u32 & ~3u);", ctr());
         println("\t\treturn;");
         println("\t}}");
         break;
@@ -790,10 +801,14 @@ bool Recompiler::Recompile(

     case PPC_INST_CNTLZD:
         println("\t{0}.u64 = {1}.u64 == 0 ? 64 : __builtin_clzll({1}.u64);", r(insn.operands[0]), r(insn.operands[1]));
+        if (strchr(insn.opcode->name, '.'))
+            println("\t{}.compare<int32_t>({}.s32, 0, {});", cr(0), r(insn.operands[0]), xer());
         break;

     case PPC_INST_CNTLZW:
         println("\t{0}.u64 = {1}.u32 == 0 ? 32 : __builtin_clz({1}.u32);", r(insn.operands[0]), r(insn.operands[1]));
+        if (strchr(insn.opcode->name, '.'))
+            println("\t{}.compare<int32_t>({}.s32, 0, {});", cr(0), r(insn.operands[0]), xer());
         break;

     case PPC_INST_DB16CYC:
@@ -828,6 +843,8 @@ bool Recompiler::Recompile(

     case PPC_INST_DIVD:
         println("\t{}.s64 = {}.s64 / {}.s64;", r(insn.operands[0]), r(insn.operands[1]), r(insn.operands[2]));
+        if (strchr(insn.opcode->name, '.'))
+            println("\t{}.compare<int32_t>({}.s32, 0, {});", cr(0), r(insn.operands[0]), xer());
         break;

     case PPC_INST_DIVDU:
@@ -1023,9 +1040,9 @@ bool Recompiler::Recompile(
         break;

     case PPC_INST_LBZU:
-        println("\t{} = {} + {}.u32;", ea(), int32_t(insn.operands[1]), r(insn.operands[2]));
-        println("\t{}.u64 = PPC_LOAD_U8({});", r(insn.operands[0]), ea());
-        println("\t{}.u32 = {};", r(insn.operands[2]), ea());
+        println("\t{}.u64 = {}.u64 + uint64_t({});", temp(), r(insn.operands[2]), int32_t(insn.operands[1]));
+        println("\t{}.u64 = PPC_LOAD_U8({}.u32);", r(insn.operands[0]), temp());
+        println("\t{}.u64 = {}.u64;", r(insn.operands[2]), temp());
         break;

     case PPC_INST_LBZX:
@@ -1043,17 +1060,17 @@ bool Recompiler::Recompile(
         break;

     case PPC_INST_LDARX:
-        print("\t{}.u64 = *(uint64_t*)(base + ", reserved());
+        print("\t{}.u64 = *(uint64_t*)(base + uint32_t(", reserved());
         if (insn.operands[1] != 0)
             print("{}.u32 + ", r(insn.operands[1]));
-        println("{}.u32);", r(insn.operands[2]));
+        println("{}.u32));", r(insn.operands[2]));
         println("\t{}.u64 = __builtin_bswap64({}.u64);", r(insn.operands[0]), reserved());
         break;

     case PPC_INST_LDU:
-        println("\t{} = {} + {}.u32;", ea(), int32_t(insn.operands[1]), r(insn.operands[2]));
-        println("\t{}.u64 = PPC_LOAD_U64({});", r(insn.operands[0]), ea());
-        println("\t{}.u32 = {};", r(insn.operands[2]), ea());
+        println("\t{}.u64 = {}.u64 + uint64_t({});", temp(), r(insn.operands[2]), int32_t(insn.operands[1]));
+        println("\t{}.u64 = PPC_LOAD_U64({}.u32);", r(insn.operands[0]), temp());
+        println("\t{}.u64 = {}.u64;", r(insn.operands[2]), temp());
         break;

     case PPC_INST_LDX:
@@ -1187,10 +1204,10 @@ bool Recompiler::Recompile(
         break;

     case PPC_INST_LWARX:
-        print("\t{}.u32 = *(uint32_t*)(base + ", reserved());
+        print("\t{}.u32 = *(uint32_t*)(base + uint32_t(", reserved());
         if (insn.operands[1] != 0)
             print("{}.u32 + ", r(insn.operands[1]));
-        println("{}.u32);", r(insn.operands[2]));
+        println("{}.u32));", r(insn.operands[2]));
         println("\t{}.u64 = __builtin_bswap32({}.u32);", r(insn.operands[0]), reserved());
         break;

@@ -1220,9 +1237,9 @@ bool Recompiler::Recompile(
         break;

     case PPC_INST_LWZU:
-        println("\t{} = {} + {}.u32;", ea(), int32_t(insn.operands[1]), r(insn.operands[2]));
-        println("\t{}.u64 = PPC_LOAD_U32({});", r(insn.operands[0]), ea());
-        println("\t{}.u32 = {};", r(insn.operands[2]), ea());
+        println("\t{}.u64 = {}.u64 + uint64_t({});", temp(), r(insn.operands[2]), int32_t(insn.operands[1]));
+        println("\t{}.u64 = PPC_LOAD_U32({}.u32);", r(insn.operands[0]), temp());
+        println("\t{}.u64 = {}.u64;", r(insn.operands[2]), temp());
         break;

     case PPC_INST_LWZX:
@@ -1260,7 +1277,9 @@ bool Recompiler::Recompile(
         break;

     case PPC_INST_MFTB:
-        println("\t{}.u64 = __rdtsc();", r(insn.operands[0]));
+        // LostOdysseyRecomp: the guest timebase must tick at the frequency the kernel
+        // reports (KeQueryPerformanceFrequency), not at the host TSC rate.
+        println("\t{}.u64 = PPCTimeBase();", r(insn.operands[0]));
         break;

     case PPC_INST_MR:
@@ -1303,6 +1322,8 @@ bool Recompiler::Recompile(

     case PPC_INST_MULHW:
         println("\t{}.s64 = (int64_t({}.s32) * int64_t({}.s32)) >> 32;", r(insn.operands[0]), r(insn.operands[1]), r(insn.operands[2]));
+        if (strchr(insn.opcode->name, '.'))
+            println("\t{}.compare<int32_t>({}.s32, 0, {});", cr(0), r(insn.operands[0]), xer());
         break;

     case PPC_INST_MULHWU:
@@ -1313,6 +1334,8 @@ bool Recompiler::Recompile(

     case PPC_INST_MULLD:
         println("\t{}.s64 = {}.s64 * {}.s64;", r(insn.operands[0]), r(insn.operands[1]), r(insn.operands[2]));
+        if (strchr(insn.opcode->name, '.'))
+            println("\t{}.compare<int32_t>({}.s32, 0, {});", cr(0), r(insn.operands[0]), xer());
         break;

     case PPC_INST_MULLI:
@@ -1327,6 +1350,8 @@ bool Recompiler::Recompile(

     case PPC_INST_NAND:
         println("\t{}.u64 = ~({}.u64 & {}.u64);", r(insn.operands[0]), r(insn.operands[1]), r(insn.operands[2]));
+        if (strchr(insn.opcode->name, '.'))
+            println("\t{}.compare<int32_t>({}.s32, 0, {});", cr(0), r(insn.operands[0]), xer());
         break;

     case PPC_INST_NEG:
@@ -1341,6 +1366,8 @@ bool Recompiler::Recompile(

     case PPC_INST_NOR:
         println("\t{}.u64 = ~({}.u64 | {}.u64);", r(insn.operands[0]), r(insn.operands[1]), r(insn.operands[2]));
+        if (strchr(insn.opcode->name, '.'))
+            println("\t{}.compare<int32_t>({}.s32, 0, {});", cr(0), r(insn.operands[0]), xer());
         break;

     case PPC_INST_NOT:
@@ -1357,6 +1384,8 @@ bool Recompiler::Recompile(

     case PPC_INST_ORC:
         println("\t{}.u64 = {}.u64 | ~{}.u64;", r(insn.operands[0]), r(insn.operands[1]), r(insn.operands[2]));
+        if (strchr(insn.opcode->name, '.'))
+            println("\t{}.compare<int32_t>({}.s32, 0, {});", cr(0), r(insn.operands[0]), xer());
         break;

     case PPC_INST_ORI:
@@ -1369,23 +1398,33 @@ bool Recompiler::Recompile(

     case PPC_INST_RLDICL:
         println("\t{}.u64 = __builtin_rotateleft64({}.u64, {}) & 0x{:X};", r(insn.operands[0]), r(insn.operands[1]), insn.operands[2], ComputeMask(insn.operands[3], 63));
+        if (strchr(insn.opcode->name, '.'))
+            println("\t{}.compare<int32_t>({}.s32, 0, {});", cr(0), r(insn.operands[0]), xer());
         break;

     case PPC_INST_RLDICR:
         println("\t{}.u64 = __builtin_rotateleft64({}.u64, {}) & 0x{:X};", r(insn.operands[0]), r(insn.operands[1]), insn.operands[2], ComputeMask(0, insn.operands[3]));
+        if (strchr(insn.opcode->name, '.'))
+            println("\t{}.compare<int32_t>({}.s32, 0, {});", cr(0), r(insn.operands[0]), xer());
         break;

     case PPC_INST_RLDIMI:
     {
         const uint64_t mask = ComputeMask(insn.operands[3], ~insn.operands[2]);
         println("\t{}.u64 = (__builtin_rotateleft64({}.u64, {}) & 0x{:X}) | ({}.u64 & 0x{:X});", r(insn.operands[0]), r(insn.operands[1]), insn.operands[2], mask, r(insn.operands[0]), ~mask);
+        if (strchr(insn.opcode->name, '.'))
+            println("\t{}.compare<int32_t>({}.s32, 0, {});", cr(0), r(insn.operands[0]), xer());
         break;
     }

     case PPC_INST_RLWIMI:
     {
         const uint64_t mask = ComputeMask(insn.operands[3] + 32, insn.operands[4] + 32);
-        println("\t{}.u64 = (__builtin_rotateleft32({}.u32, {}) & 0x{:X}) | ({}.u64 & 0x{:X});", r(insn.operands[0]), r(insn.operands[1]), insn.operands[2], mask, r(insn.operands[0]), ~mask);
+        // ROTL32 duplicates the low word; a wrapping mask also selects bits
+        // from the high word of that 64-bit rotated value.
+        println("\t{}.u64 = (__builtin_rotateleft64({}.u32 | ({}.u64 << 32), {}) & 0x{:X}) | ({}.u64 & 0x{:X});", r(insn.operands[0]), r(insn.operands[1]), r(insn.operands[1]), insn.operands[2], mask, r(insn.operands[0]), ~mask);
+        if (strchr(insn.opcode->name, '.'))
+            println("\t{}.compare<int32_t>({}.s32, 0, {});", cr(0), r(insn.operands[0]), xer());
         break;
     }

@@ -1397,10 +1436,14 @@ bool Recompiler::Recompile(

     case PPC_INST_ROTLDI:
         println("\t{}.u64 = __builtin_rotateleft64({}.u64, {});", r(insn.operands[0]), r(insn.operands[1]), insn.operands[2]);
+        if (strchr(insn.opcode->name, '.'))
+            println("\t{}.compare<int32_t>({}.s32, 0, {});", cr(0), r(insn.operands[0]), xer());
         break;

     case PPC_INST_ROTLW:
         println("\t{}.u64 = __builtin_rotateleft32({}.u32, {}.u8 & 0x1F);", r(insn.operands[0]), r(insn.operands[1]), r(insn.operands[2]));
+        if (strchr(insn.opcode->name, '.'))
+            println("\t{}.compare<int32_t>({}.s32, 0, {});", cr(0), r(insn.operands[0]), xer());
         break;

     case PPC_INST_ROTLWI:
@@ -1411,6 +1454,8 @@ bool Recompiler::Recompile(

     case PPC_INST_SLD:
         println("\t{}.u64 = {}.u8 & 0x40 ? 0 : ({}.u64 << ({}.u8 & 0x7F));", r(insn.operands[0]), r(insn.operands[2]), r(insn.operands[1]), r(insn.operands[2]));
+        if (strchr(insn.opcode->name, '.'))
+            println("\t{}.compare<int32_t>({}.s32, 0, {});", cr(0), r(insn.operands[0]), xer());
         break;

     case PPC_INST_SLW:
@@ -1421,9 +1466,10 @@ bool Recompiler::Recompile(

     case PPC_INST_SRAD:
         println("\t{}.u64 = {}.u64 & 0x7F;", temp(), r(insn.operands[2]));
-        println("\tif ({}.u64 > 0x3F) {}.u64 = 0x3F;", temp(), temp());
-        println("\t{}.ca = ({}.s64 < 0) & ((({}.s64 >> {}.u64) << {}.u64) != {}.s64);", xer(), r(insn.operands[1]), r(insn.operands[1]), temp(), temp(), r(insn.operands[1]));
-        println("\t{}.s64 = {}.s64 >> {}.u64;", r(insn.operands[0]), r(insn.operands[1]), temp());
+        println("\t{}.ca = ({}.s64 < 0) && ({}.u64 >= 64 || ({}.u64 & ((uint64_t(1) << {}.u64) - 1)) != 0);", xer(), r(insn.operands[1]), temp(), r(insn.operands[1]), temp());
+        println("\t{}.s64 = {}.u64 >= 64 ? ({}.s64 < 0 ? -1 : 0) : ({}.s64 >> {}.u64);", r(insn.operands[0]), temp(), r(insn.operands[1]), r(insn.operands[1]), temp());
+        if (strchr(insn.opcode->name, '.'))
+            println("\t{}.compare<int32_t>({}.s32, 0, {});", cr(0), r(insn.operands[0]), xer());
         break;

     case PPC_INST_SRADI:
@@ -1437,13 +1483,14 @@ bool Recompiler::Recompile(
             println("\t{}.ca = 0;", xer());
             println("\t{}.s64 = {}.s64;", r(insn.operands[0]), r(insn.operands[1]));
         }
+        if (strchr(insn.opcode->name, '.'))
+            println("\t{}.compare<int32_t>({}.s32, 0, {});", cr(0), r(insn.operands[0]), xer());
         break;

     case PPC_INST_SRAW:
         println("\t{}.u32 = {}.u32 & 0x3F;", temp(), r(insn.operands[2]));
-        println("\tif ({}.u32 > 0x1F) {}.u32 = 0x1F;", temp(), temp());
-        println("\t{}.ca = ({}.s32 < 0) & ((({}.s32 >> {}.u32) << {}.u32) != {}.s32);", xer(), r(insn.operands[1]), r(insn.operands[1]), temp(), temp(), r(insn.operands[1]));
-        println("\t{}.s64 = {}.s32 >> {}.u32;", r(insn.operands[0]), r(insn.operands[1]), temp());
+        println("\t{}.ca = ({}.s32 < 0) && ({}.u32 >= 32 || ({}.u32 & ((uint32_t(1) << {}.u32) - 1)) != 0);", xer(), r(insn.operands[1]), temp(), r(insn.operands[1]), temp());
+        println("\t{}.s64 = {}.u32 >= 32 ? ({}.s32 < 0 ? -1 : 0) : ({}.s32 >> {}.u32);", r(insn.operands[0]), temp(), r(insn.operands[1]), r(insn.operands[1]), temp());
         if (strchr(insn.opcode->name, '.'))
             println("\t{}.compare<int32_t>({}.s32, 0, {});", cr(0), r(insn.operands[0]), xer());
         break;
@@ -1465,6 +1512,8 @@ bool Recompiler::Recompile(

     case PPC_INST_SRD:
         println("\t{}.u64 = {}.u8 & 0x40 ? 0 : ({}.u64 >> ({}.u8 & 0x7F));", r(insn.operands[0]), r(insn.operands[2]), r(insn.operands[1]), r(insn.operands[2]));
+        if (strchr(insn.opcode->name, '.'))
+            println("\t{}.compare<int32_t>({}.s32, 0, {});", cr(0), r(insn.operands[0]), xer());
         break;

     case PPC_INST_SRW:
@@ -1481,9 +1530,9 @@ bool Recompiler::Recompile(
         break;

     case PPC_INST_STBU:
-        println("\t{} = {} + {}.u32;", ea(), int32_t(insn.operands[1]), r(insn.operands[2]));
-        println("\tPPC_STORE_U8({}, {}.u8);", ea(), r(insn.operands[0]));
-        println("\t{}.u32 = {};", r(insn.operands[2]), ea());
+        println("\t{}.u64 = {}.u64 + uint64_t({});", temp(), r(insn.operands[2]), int32_t(insn.operands[1]));
+        println("\tPPC_STORE_U8({}.u32, {}.u8);", temp(), r(insn.operands[0]));
+        println("\t{}.u64 = {}.u64;", r(insn.operands[2]), temp());
         break;

     case PPC_INST_STBX:
@@ -1503,17 +1552,17 @@ bool Recompiler::Recompile(
     case PPC_INST_STDCX:
         println("\t{}.lt = 0;", cr(0));
         println("\t{}.gt = 0;", cr(0));
-        print("\t{}.eq = __sync_bool_compare_and_swap(reinterpret_cast<uint64_t*>(base + ", cr(0));
+        print("\t{}.eq = __sync_bool_compare_and_swap(reinterpret_cast<uint64_t*>(base + uint32_t(", cr(0));
         if (insn.operands[1] != 0)
             print("{}.u32 + ", r(insn.operands[1]));
-        println("{}.u32), {}.s64, __builtin_bswap64({}.s64));", r(insn.operands[2]), reserved(), r(insn.operands[0]));
+        println("{}.u32)), {}.s64, __builtin_bswap64({}.s64));", r(insn.operands[2]), reserved(), r(insn.operands[0]));
         println("\t{}.so = {}.so;", cr(0), xer());
         break;

     case PPC_INST_STDU:
-        println("\t{} = {} + {}.u32;", ea(), int32_t(insn.operands[1]), r(insn.operands[2]));
-        println("\tPPC_STORE_U64({}, {}.u64);", ea(), r(insn.operands[0]));
-        println("\t{}.u32 = {};", r(insn.operands[2]), ea());
+        println("\t{}.u64 = {}.u64 + uint64_t({});", temp(), r(insn.operands[2]), int32_t(insn.operands[1]));
+        println("\tPPC_STORE_U64({}.u32, {}.u64);", temp(), r(insn.operands[0]));
+        println("\t{}.u64 = {}.u64;", r(insn.operands[2]), temp());
         break;

     case PPC_INST_STDX:
@@ -1658,23 +1707,23 @@ bool Recompiler::Recompile(
     case PPC_INST_STWCX:
         println("\t{}.lt = 0;", cr(0));
         println("\t{}.gt = 0;", cr(0));
-        print("\t{}.eq = __sync_bool_compare_and_swap(reinterpret_cast<uint32_t*>(base + ", cr(0));
+        print("\t{}.eq = __sync_bool_compare_and_swap(reinterpret_cast<uint32_t*>(base + uint32_t(", cr(0));
         if (insn.operands[1] != 0)
             print("{}.u32 + ", r(insn.operands[1]));
-        println("{}.u32), {}.s32, __builtin_bswap32({}.s32));", r(insn.operands[2]), reserved(), r(insn.operands[0]));
+        println("{}.u32)), {}.s32, __builtin_bswap32({}.s32));", r(insn.operands[2]), reserved(), r(insn.operands[0]));
         println("\t{}.so = {}.so;", cr(0), xer());
         break;

     case PPC_INST_STWU:
-        println("\t{} = {} + {}.u32;", ea(), int32_t(insn.operands[1]), r(insn.operands[2]));
-        println("\tPPC_STORE_U32({}, {}.u32);", ea(), r(insn.operands[0]));
-        println("\t{}.u32 = {};", r(insn.operands[2]), ea());
+        println("\t{}.u64 = {}.u64 + uint64_t({});", temp(), r(insn.operands[2]), int32_t(insn.operands[1]));
+        println("\tPPC_STORE_U32({}.u32, {}.u32);", temp(), r(insn.operands[0]));
+        println("\t{}.u64 = {}.u64;", r(insn.operands[2]), temp());
         break;

     case PPC_INST_STWUX:
-        println("\t{} = {}.u32 + {}.u32;", ea(), r(insn.operands[1]), r(insn.operands[2]));
-        println("\tPPC_STORE_U32({}, {}.u32);", ea(), r(insn.operands[0]));
-        println("\t{}.u32 = {};", r(insn.operands[1]), ea());
+        println("\t{}.u64 = {}.u64 + {}.u64;", temp(), r(insn.operands[1]), r(insn.operands[2]));
+        println("\tPPC_STORE_U32({}.u32, {}.u32);", temp(), r(insn.operands[0]));
+        println("\t{}.u64 = {}.u64;", r(insn.operands[1]), temp());
         break;

     case PPC_INST_STWX:
@@ -1877,6 +1926,8 @@ bool Recompiler::Recompile(

     case PPC_INST_VCMPGTUH:
         println("\tsimde_mm_store_si128((simde__m128i*){}.u8, simde_mm_cmpgt_epu16(simde_mm_load_si128((simde__m128i*){}.u16), simde_mm_load_si128((simde__m128i*){}.u16)));", v(insn.operands[0]), v(insn.operands[1]), v(insn.operands[2]));
+        if (strchr(insn.opcode->name, '.'))
+            println("\t{}.setFromMask(simde_mm_load_si128((simde__m128i*){}.u8), 0xFFFF);", cr(6), v(insn.operands[0]));
         break;

     case PPC_INST_VEXPTEFP:
@@ -2212,6 +2263,38 @@ bool Recompiler::Recompile(
             println("\t{} = {};", v(insn.operands[0]), vTemp());
             break;

+        // LostOdysseyRecomp: remaining D3D unpack types, lane mapping per Xenia
+        // (ppc_emit_altivec.cc / x64_seq_vector.cc). Packed data sits in the last
+        // PPC word(s) (host u32[0], u32[1]); results go to x..w = host f32[3..0].
+        case 2: // 2_10_10_10 in the last word: xyz signed 10-bit as 3.0 + n*2^-22, w unsigned 2-bit as 1.0 + n
+            println("\t{}.u32 = {}.u32[0];", temp(), v(insn.operands[1]));
+            println("\t{}.s32[3] = 0x40400000 + (int32_t({}.u32 << 22) >> 22);", vTemp(), temp());
+            println("\t{}.s32[2] = 0x40400000 + (int32_t({}.u32 << 12) >> 22);", vTemp(), temp());
+            println("\t{}.s32[1] = 0x40400000 + (int32_t({}.u32 << 2) >> 22);", vTemp(), temp());
+            println("\t{}.u32[0] = 0x3F800000 + ({}.u32 >> 30);", vTemp(), temp());
+            println("\t{} = {};", v(insn.operands[0]), vTemp());
+            break;
+
+        case 3: // 2 half floats in the last word -> (x, y, 0, 1)
+            println("\t{}.f32[3] = PPCHalfToFloat({}.u16[1]);", vTemp(), v(insn.operands[1]));
+            println("\t{}.f32[2] = PPCHalfToFloat({}.u16[0]);", vTemp(), v(insn.operands[1]));
+            println("\t{}.f32[1] = 0.0f;", vTemp());
+            println("\t{}.f32[0] = 1.0f;", vTemp());
+            println("\t{} = {};", v(insn.operands[0]), vTemp());
+            break;
+
+        case 4: // 4 shorts in the last two words: 3.0 + n*2^-22
+            for (size_t i = 0; i < 4; i++)
+                println("\t{}.s32[{}] = 0x40400000 + {}.s16[{}];", vTemp(), i, v(insn.operands[1]), i);
+            println("\t{} = {};", v(insn.operands[0]), vTemp());
+            break;
+
+        case 5: // 4 half floats in the last two words
+            for (size_t i = 0; i < 4; i++)
+                println("\t{}.f32[{}] = PPCHalfToFloat({}.u16[{}]);", vTemp(), i, v(insn.operands[1]), i);
+            println("\t{} = {};", v(insn.operands[0]), vTemp());
+            break;
+
         default:
             println("\t__builtin_debugtrap();");
             break;
@@ -2263,6 +2346,165 @@ bool Recompiler::Recompile(
         println("\t{}.u64 = {}.u64 ^ {};", r(insn.operands[0]), r(insn.operands[1]), insn.operands[2] << 16);
         break;

+    // ---- LostOdysseyRecomp additions: instructions absent from upstream XenonRecomp ----
+    // Vector element order in PPCVRegister is reversed relative to PPC (host u8[0] == PPC byte 15),
+    // matching the existing VPKSHUS convention of passing (vB, vA) to pack intrinsics.
+
+    case PPC_INST_VSLH:
+        for (size_t i = 0; i < 8; i++)
+            println("\t{}.u16[{}] = {}.u16[{}] << ({}.u16[{}] & 0xF);", v(insn.operands[0]), i, v(insn.operands[1]), i, v(insn.operands[2]), i);
+        break;
+
+    case PPC_INST_VSRH:
+        for (size_t i = 0; i < 8; i++)
+            println("\t{}.u16[{}] = {}.u16[{}] >> ({}.u16[{}] & 0xF);", v(insn.operands[0]), i, v(insn.operands[1]), i, v(insn.operands[2]), i);
+        break;
+
+    case PPC_INST_VSRAH:
+        for (size_t i = 0; i < 8; i++)
+            println("\t{}.s16[{}] = {}.s16[{}] >> ({}.u16[{}] & 0xF);", v(insn.operands[0]), i, v(insn.operands[1]), i, v(insn.operands[2]), i);
+        break;
+
+    case PPC_INST_VSRAB:
+        for (size_t i = 0; i < 16; i++)
+            println("\t{}.s8[{}] = {}.s8[{}] >> ({}.u8[{}] & 0x7);", v(insn.operands[0]), i, v(insn.operands[1]), i, v(insn.operands[2]), i);
+        break;
+
+    case PPC_INST_VRLH:
+        for (size_t i = 0; i < 8; i++)
+        {
+            println("\t{}.u32 = {}.u16[{}] & 0xF;", temp(), v(insn.operands[2]), i);
+            println("\t{}.u16[{}] = uint16_t(({}.u16[{}] << {}.u32) | ({}.u16[{}] >> ((16 - {}.u32) & 0xF)));", v(insn.operands[0]), i, v(insn.operands[1]), i, temp(), v(insn.operands[1]), i, temp());
+        }
+        break;
+
+    case PPC_INST_VSUBSHS:
+        println("\tsimde_mm_store_si128((simde__m128i*){}.s16, simde_mm_subs_epi16(simde_mm_load_si128((simde__m128i*){}.s16), simde_mm_load_si128((simde__m128i*){}.s16)));", v(insn.operands[0]), v(insn.operands[1]), v(insn.operands[2]));
+        break;
+
+    case PPC_INST_VSUBUBM:
+        println("\tsimde_mm_store_si128((simde__m128i*){}.u8, simde_mm_sub_epi8(simde_mm_load_si128((simde__m128i*){}.u8), simde_mm_load_si128((simde__m128i*){}.u8)));", v(insn.operands[0]), v(insn.operands[1]), v(insn.operands[2]));
+        break;
+
+    case PPC_INST_VADDSBS:
+        println("\tsimde_mm_store_si128((simde__m128i*){}.s8, simde_mm_adds_epi8(simde_mm_load_si128((simde__m128i*){}.s8), simde_mm_load_si128((simde__m128i*){}.s8)));", v(insn.operands[0]), v(insn.operands[1]), v(insn.operands[2]));
+        break;
+
+    case PPC_INST_VADDSWS:
+        // TODO: vectorize
+        for (size_t i = 0; i < 4; i++)
+        {
+            println("\t{}.s64 = int64_t({}.s32[{}]) + int64_t({}.s32[{}]);", temp(), v(insn.operands[1]), i, v(insn.operands[2]), i);
+            println("\t{}.s32[{}] = {}.s64 > INT_MAX ? INT_MAX : {}.s64 < INT_MIN ? INT_MIN : {}.s64;", v(insn.operands[0]), i, temp(), temp(), temp());
+        }
+        break;
+
+    case PPC_INST_VSPLTISH:
+        println("\tsimde_mm_store_si128((simde__m128i*){}.u16, simde_mm_set1_epi16(short(0x{:X})));", v(insn.operands[0]), insn.operands[1]);
+        break;
+
+    case PPC_INST_VANDC:
+        println("\tsimde_mm_store_si128((simde__m128i*){}.u8, simde_mm_andnot_si128(simde_mm_load_si128((simde__m128i*){}.u8), simde_mm_load_si128((simde__m128i*){}.u8)));", v(insn.operands[0]), v(insn.operands[2]), v(insn.operands[1]));
+        break;
+
+    case PPC_INST_VNOR:
+        println("\tsimde_mm_store_si128((simde__m128i*){}.u8, simde_mm_xor_si128(simde_mm_or_si128(simde_mm_load_si128((simde__m128i*){}.u8), simde_mm_load_si128((simde__m128i*){}.u8)), simde_mm_set1_epi32(-1)));", v(insn.operands[0]), v(insn.operands[1]), v(insn.operands[2]));
+        break;
+
+    case PPC_INST_VMAXSH:
+        println("\tsimde_mm_store_si128((simde__m128i*){}.s16, simde_mm_max_epi16(simde_mm_load_si128((simde__m128i*){}.s16), simde_mm_load_si128((simde__m128i*){}.s16)));", v(insn.operands[0]), v(insn.operands[1]), v(insn.operands[2]));
+        break;
+
+    case PPC_INST_VMINSH:
+        println("\tsimde_mm_store_si128((simde__m128i*){}.s16, simde_mm_min_epi16(simde_mm_load_si128((simde__m128i*){}.s16), simde_mm_load_si128((simde__m128i*){}.s16)));", v(insn.operands[0]), v(insn.operands[1]), v(insn.operands[2]));
+        break;
+
+    case PPC_INST_VAVGUH:
+        println("\tsimde_mm_store_si128((simde__m128i*){}.u16, simde_mm_avg_epu16(simde_mm_load_si128((simde__m128i*){}.u16), simde_mm_load_si128((simde__m128i*){}.u16)));", v(insn.operands[0]), v(insn.operands[1]), v(insn.operands[2]));
+        break;
+
+    case PPC_INST_VCMPGTSH:
+        println("\tsimde_mm_store_si128((simde__m128i*){}.u8, simde_mm_cmpgt_epi16(simde_mm_load_si128((simde__m128i*){}.s16), simde_mm_load_si128((simde__m128i*){}.s16)));", v(insn.operands[0]), v(insn.operands[1]), v(insn.operands[2]));
+        if (strchr(insn.opcode->name, '.'))
+            println("\t{}.setFromMask(simde_mm_load_si128((simde__m128i*){}.u8), 0xFFFF);", cr(6), v(insn.operands[0]));
+        break;
+
+    case PPC_INST_VCMPGTSW:
+        println("\tsimde_mm_store_si128((simde__m128i*){}.u8, simde_mm_cmpgt_epi32(simde_mm_load_si128((simde__m128i*){}.s32), simde_mm_load_si128((simde__m128i*){}.s32)));", v(insn.operands[0]), v(insn.operands[1]), v(insn.operands[2]));
+        if (strchr(insn.opcode->name, '.'))
+            println("\t{}.setFromMask(simde_mm_load_si128((simde__m128i*){}.u8), 0xFFFF);", cr(6), v(insn.operands[0]));
+        break;
+
+    case PPC_INST_VCMPEQUH:
+        println("\tsimde_mm_store_si128((simde__m128i*){}.u8, simde_mm_cmpeq_epi16(simde_mm_load_si128((simde__m128i*){}.u16), simde_mm_load_si128((simde__m128i*){}.u16)));", v(insn.operands[0]), v(insn.operands[1]), v(insn.operands[2]));
+        if (strchr(insn.opcode->name, '.'))
+            println("\t{}.setFromMask(simde_mm_load_si128((simde__m128i*){}.u8), 0xFFFF);", cr(6), v(insn.operands[0]));
+        break;
+
+    case PPC_INST_VCTUXS:
+        // float -> unsigned int, saturating, scaled by 2^UIMM; NaN -> 0
+        printSetFlushMode(true);
+        for (size_t i = 0; i < 4; i++)
+        {
+            println("\t{}.f64 = double({}.f32[{}]) * {};", temp(), v(insn.operands[1]), i, double(1ull << insn.operands[2]));
+            println("\t{}.u32[{}] = {}.f64 >= 4294967295.0 ? 0xFFFFFFFFu : ({}.f64 > 0.0 ? uint32_t({}.f64) : 0u);", v(insn.operands[0]), i, temp(), temp(), temp());
+        }
+        break;
+
+    case PPC_INST_VPKSWSS:
+        println("\tsimde_mm_store_si128((simde__m128i*){}.s16, simde_mm_packs_epi32(simde_mm_load_si128((simde__m128i*){}.s32), simde_mm_load_si128((simde__m128i*){}.s32)));", v(insn.operands[0]), v(insn.operands[2]), v(insn.operands[1]));
+        break;
+
+    case PPC_INST_VPKSWUS:
+        println("\tsimde_mm_store_si128((simde__m128i*){}.u16, simde_mm_packus_epi32(simde_mm_load_si128((simde__m128i*){}.s32), simde_mm_load_si128((simde__m128i*){}.s32)));", v(insn.operands[0]), v(insn.operands[2]), v(insn.operands[1]));
+        break;
+
+    case PPC_INST_VPKSHSS:
+        println("\tsimde_mm_store_si128((simde__m128i*){}.s8, simde_mm_packs_epi16(simde_mm_load_si128((simde__m128i*){}.s16), simde_mm_load_si128((simde__m128i*){}.s16)));", v(insn.operands[0]), v(insn.operands[2]), v(insn.operands[1]));
+        break;
+
+    case PPC_INST_VPKUHUS:
+        // unsigned halfword -> unsigned byte with saturation: clamp to 255 first so packus treats inputs as non-negative
+        println("\tsimde_mm_store_si128((simde__m128i*){}.u8, simde_mm_packus_epi16(simde_mm_min_epu16(simde_mm_load_si128((simde__m128i*){}.u16), simde_mm_set1_epi16(0xFF)), simde_mm_min_epu16(simde_mm_load_si128((simde__m128i*){}.u16), simde_mm_set1_epi16(0xFF))));", v(insn.operands[0]), v(insn.operands[2]), v(insn.operands[1]));
+        break;
+
+    case PPC_INST_VPKUWUS:
+        println("\tsimde_mm_store_si128((simde__m128i*){}.u16, simde_mm_packus_epi32(simde_mm_min_epu32(simde_mm_load_si128((simde__m128i*){}.u32), simde_mm_set1_epi32(0xFFFF)), simde_mm_min_epu32(simde_mm_load_si128((simde__m128i*){}.u32), simde_mm_set1_epi32(0xFFFF))));", v(insn.operands[0]), v(insn.operands[2]), v(insn.operands[1]));
+        break;
+
+    case PPC_INST_MULHD:
+        println("\t{}.s64 = int64_t((__int128_t({}.s64) * __int128_t({}.s64)) >> 64);", r(insn.operands[0]), r(insn.operands[1]), r(insn.operands[2]));
+        if (strchr(insn.opcode->name, '.'))
+            println("\t{}.compare<int32_t>({}.s32, 0, {});", cr(0), r(insn.operands[0]), xer());
+        break;
+
+    case PPC_INST_MULHDU:
+        println("\t{}.u64 = uint64_t((__uint128_t({}.u64) * __uint128_t({}.u64)) >> 64);", r(insn.operands[0]), r(insn.operands[1]), r(insn.operands[2]));
+        if (strchr(insn.opcode->name, '.'))
+            println("\t{}.compare<int32_t>({}.s32, 0, {});", cr(0), r(insn.operands[0]), xer());
+        break;
+
+    case PPC_INST_EQV:
+        println("\t{}.u64 = ~({}.u64 ^ {}.u64);", r(insn.operands[0]), r(insn.operands[1]), r(insn.operands[2]));
+        if (strchr(insn.opcode->name, '.'))
+            println("\t{}.compare<int32_t>({}.s32, 0, {});", cr(0), r(insn.operands[0]), xer());
+        break;
+
+    case PPC_INST_CROR:
+    case PPC_INST_CRORC:
+    {
+        static const char* crFields[] = { "lt", "gt", "eq", "so" };
+        const uint32_t d = insn.operands[0], a = insn.operands[1], b = insn.operands[2];
+        println("\t{}.{} = {}.{} | {}{}.{};", cr(d / 4), crFields[d % 4], cr(a / 4), crFields[a % 4],
+            insn.opcode->id == PPC_INST_CRORC ? "!" : "", cr(b / 4), crFields[b % 4]);
+        break;
+    }
+
+    case PPC_INST_FRSQRTE:
+        printSetFlushMode(false);
+        println("\t{}.f64 = 1.0 / sqrt({}.f64);", f(insn.operands[0]), f(insn.operands[1]));
+        break;
+
     default:
         return false;
     }
diff --git a/XenonUtils/ppc_context.h b/XenonUtils/ppc_context.h
index 31a86cd3e970c0ceebbcd94a9c06137ad817e1ea..b573187aa282a65209d206f7e44f3f5b1900cb20 100644
--- a/XenonUtils/ppc_context.h
+++ b/XenonUtils/ppc_context.h
@@ -8,6 +8,7 @@
 #include <climits>
 #include <cmath>
 #include <csetjmp>
+#include <chrono>
 #include <cstdint>
 #include <cstdlib>
 #include <cstring>
@@ -32,19 +33,19 @@
 #define PPC_FUNC_PROLOGUE() __builtin_assume(((size_t)base & 0x1F) == 0)

 #ifndef PPC_LOAD_U8
-#define PPC_LOAD_U8(x) *(volatile uint8_t*)(base + (x))
+#define PPC_LOAD_U8(x) *(volatile uint8_t*)(base + uint32_t(x))
 #endif

 #ifndef PPC_LOAD_U16
-#define PPC_LOAD_U16(x) __builtin_bswap16(*(volatile uint16_t*)(base + (x)))
+#define PPC_LOAD_U16(x) __builtin_bswap16(*(volatile uint16_t*)(base + uint32_t(x)))
 #endif

 #ifndef PPC_LOAD_U32
-#define PPC_LOAD_U32(x) __builtin_bswap32(*(volatile uint32_t*)(base + (x)))
+#define PPC_LOAD_U32(x) __builtin_bswap32(*(volatile uint32_t*)(base + uint32_t(x)))
 #endif

 #ifndef PPC_LOAD_U64
-#define PPC_LOAD_U64(x) __builtin_bswap64(*(volatile uint64_t*)(base + (x)))
+#define PPC_LOAD_U64(x) __builtin_bswap64(*(volatile uint64_t*)(base + uint32_t(x)))
 #endif

 // TODO: Implement.
@@ -68,19 +69,19 @@
 #endif

 #ifndef PPC_STORE_U8
-#define PPC_STORE_U8(x, y) *(volatile uint8_t*)(base + (x)) = (y)
+#define PPC_STORE_U8(x, y) *(volatile uint8_t*)(base + uint32_t(x)) = (y)
 #endif

 #ifndef PPC_STORE_U16
-#define PPC_STORE_U16(x, y) *(volatile uint16_t*)(base + (x)) = __builtin_bswap16(y)
+#define PPC_STORE_U16(x, y) *(volatile uint16_t*)(base + uint32_t(x)) = __builtin_bswap16(y)
 #endif

 #ifndef PPC_STORE_U32
-#define PPC_STORE_U32(x, y) *(volatile uint32_t*)(base + (x)) = __builtin_bswap32(y)
+#define PPC_STORE_U32(x, y) *(volatile uint32_t*)(base + uint32_t(x)) = __builtin_bswap32(y)
 #endif

 #ifndef PPC_STORE_U64
-#define PPC_STORE_U64(x, y) *(volatile uint64_t*)(base + (x)) = __builtin_bswap64(y)
+#define PPC_STORE_U64(x, y) *(volatile uint64_t*)(base + uint32_t(x)) = __builtin_bswap64(y)
 #endif

 // MMIO Store handling is completely reliant on being preeceded by eieio.
@@ -105,6 +106,46 @@
 #define PPC_CALL_FUNC(x) x(ctx, base)
 #endif

+// LostOdysseyRecomp: guest timebase (mftb). The Xbox 360 timebase runs at
+// 49.875 MHz and titles divide mftb deltas by KeQueryPerformanceFrequency, so
+// the two must agree; derive it from the host monotonic clock instead of the
+// host TSC (which ticks ~70x faster and made the game run at that speed).
+#define PPC_TIMEBASE_FREQUENCY 49875000ull
+inline uint64_t PPCTimeBase()
+{
+    const auto now = std::chrono::steady_clock::now().time_since_epoch();
+    const auto ns = std::chrono::duration_cast<std::chrono::nanoseconds>(now).count();
+    return uint64_t(ns / 1000000000) * PPC_TIMEBASE_FREQUENCY +
+           (uint64_t(ns % 1000000000) * PPC_TIMEBASE_FREQUENCY) / 1000000000ull;
+}
+
+// LostOdysseyRecomp: IEEE half -> float, used by the vupkd3d128 unpack types.
+inline float PPCHalfToFloat(uint16_t h)
+{
+    uint32_t sign = uint32_t(h & 0x8000) << 16;
+    uint32_t exp = (h >> 10) & 0x1F;
+    uint32_t mant = h & 0x3FF;
+    uint32_t bits;
+    if (exp == 0)
+    {
+        if (mant == 0)
+            bits = sign;
+        else
+        {
+            exp = 113;
+            while (!(mant & 0x400)) { mant <<= 1; exp--; }
+            bits = sign | (exp << 23) | ((mant & 0x3FF) << 13);
+        }
+    }
+    else if (exp == 31)
+        bits = sign | 0x7F800000 | (mant << 13);
+    else
+        bits = sign | ((exp + 112) << 23) | (mant << 13);
+    float f;
+    memcpy(&f, &bits, 4);
+    return f;
+}
+
 #define PPC_MEMORY_SIZE 0x100000000ull

 #define PPC_LOOKUP_FUNC(x, y) *(PPCFunc**)(x + PPC_IMAGE_BASE + PPC_IMAGE_SIZE + (uint64_t(uint32_t(y) - PPC_CODE_BASE) * 2))
